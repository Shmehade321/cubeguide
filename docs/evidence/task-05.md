# T05 — Session, storage and recovery

Status: running. Base: 436cd84. R05/R09–R11/R18; V09/V11. T04 automated work is committed; physical solver qualification remains open under the plan's independent-work allowance.

## Increment 1 — consent, revisions and action planning

The new CubeSession module implements the initial workflow through action preparation and comparison: explicit manual start/replacement, real legality classification, consent/decline, retained confirmed input, cancellation, stale-result rejection, wrong-original rejection, typed solve failures and one extended timeout retry. Revision overflow is bounded; cancellation remains possible at the last revision without allowing a later stale result. A real service integration test consumes an emitted solve command and returns a verified plan.

The transition table currently covers all nine implemented phases against ten event forms (90 combinations). Later guide, saving, completion, scan and recovery events/phases must expand that table; this is not a claim that the full specification's state machine is finished. Action preparation computes an immutable pending action, and interruption retains its identity for comparison. No event yet enables preview or physical advancement: durable preparation/acknowledgement are the next increment.

GuidePlanner uses the 24-pose graph in the specified neighbor order, emits minimal acknowledged regrips followed by a front-face demonstration, and preserves canonical puzzle state during regrips. All 24×18 combinations reconstruct the original move; literal identity-pose paths check stable tie-breaking. Action IDs bound the solver index to 0…29 and action index to 0…2, including decoding. The initial identity pose is a display reference; the guide must still require explicit physical alignment before preview in the guidance increment.

Behavioral RED/GREEN observed for starting/replacing work, revision limits, classification/consent, stale/results/retry transitions, planner paths/identity bounds, front demonstrations and preparation interruptions. A first transition-table literal accidentally rejected offer→consent; corrected against the written contract, not counted as a product regression. While wiring interruption, a guard was initially changed on edit instead of cancel; the matrix and interruption tests caught it and the implementation was corrected. The front-demonstration property was initially drafted with the type scaffold; that draft was removed, a nil callable boundary produced behavioral RED, then the derivation was rewritten. No such draft was committed.

Full PR regression passed for this increment. This increment is not the T05 exit: durable preparation and acknowledgements, persistent snapshots, atomic storage/fault injection, before/after/uncertain recovery, deletion and complete lifecycle/state-event coverage remain required. The application has no session UI integration yet. No public shipment is claimed.

## Next implementation constraints

- Extend the shared Session reducer; do not create a second independent UI workflow model. Replace the preparation command with a concrete durable-save payload before exposing any preview/acknowledgement event.
- Reconstruct guidance deterministically from the original verified plan and initial reference pose. After a regrip, preserve the original per-move action numbering; recomputing only from the updated pose must not silently renumber the pending action. A bounded full action sequence (at most 90 entries for 30 moves) is one straightforward representation to verify.
- Physical alignment is explicit. The initial identity pose is a required holding reference, not evidence of the user's actual pose. Persist/validate alignment and guard preview accordingly.
- Store before enabling preview, then store acknowledged pose/index before the following action. Duplicate/stale callbacks must preserve the last durable state; save failures enter physical comparison before retry.
- Persist and bound semantic center/display mapping outside mathematical identity. Manual input must supply explicit centers; never infer a manufacturer palette when adding the editor.
- The final T05 exit still requires all specified workflow pairs, real filesystem crash/write/locked/corrupt-schema tests, restore/replay, before/after/uncertain decisions, cancellation/deletion generations and late-write protection.

Ruling: expand the state/event matrix as each planner/persistence-dependent state becomes implementable, rather than claiming all future states pass from placeholder transitions. The complete specification matrix is still mandatory at T05 exit. Cost if wrong: missed interactions until the final matrix expansion; explicit enum coverage currently forces each added phase into the table.

Interface mapping for this module: `SessionReducer.reduce(_:event:)` implements the planned pure reducer; `GuidePlanner.actions(for:at:state:sessionRevision:moveIndex:)` creates bounded ActionIDs internally. All consumers must use these shared types. Initial action preparation remains a command boundary until the storage increment supplies its durable payload and acknowledgement events.


## Increment 1 regression

`SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-workflow-pr Scripts/test-pr.sh` exited 0: 81 Swift package tests (17 new session/planner tests), 34 Python tests and three simulator app/UI tests, with no skipped or failed tests. The unchanged solver also passed the complete 10,000-state PR corpus, 46,741 shallow states, eight named cases and pinned reference comparisons. All non-document source hashes match the pre-test snapshot. Reports/red transcripts and package output are under `t05/`; full local artifacts are under `Artifacts/t05-workflow-pr/`.

This is a committed coherent T05 increment, not T05 completion or an implemented guide UI. Next: durable action preparation, explicit alignment, exactly-once acknowledgements and bounded storage/recovery.

## Increment 2 — durable-save protocol and physical comparison

The reducer now emits concrete GuideSaveRequest candidates. Preparation must succeed before alignment/play; acknowledgement retains the previous progress until its matching save succeeds. The next action requires a separate preparation save. Immutable GuideProgress reconstructs the full timeline from the verified original plan, preserving action IDs across regrips and restored offsets. Playback IDs reject late completion callbacks; preview completion never acknowledges a physical action.

Before/after/uncertain comparison handles interrupted preparation, interrupted acknowledgement and failed writes. Comparison retries use a new save ID. Backgrounding stops preview and removes alignment permission; a pending write settles before a next action can be prepared. A failed save retains the prior reducer progress. This is a callback protocol, not yet proof of filesystem durability: the real atomic store, crash injection and restore validation remain outstanding.

Explicit CenterPalette preserves six user-supplied semantic center names in URFDLB order, separately from mathematical face identity. Construction and decoding reject missing/duplicate/unknown colors; decoding reads at most six entries. No manufacturer arrangement is assumed. Captured display swatches and editor integration remain outstanding.

Behavioral RED/GREEN transcripts: `Artifacts/t05-progress-*.log`, `Artifacts/t05-save-*.log`, `Artifacts/t05-state-protocol-*.log`, and `Artifacts/t05-palette-{red,green}.log`. Focused session protocol run passed 28 tests; palette run passed two more. The expanded workflow matrix covers 14 phases × ten original events; the persistence/playback matrix covers 19 phase/substate fixtures × eleven events. These cover implemented states, not the final camera/completion/deletion matrix.

Ruling refining the prior alignment note: durable pose describes the acknowledged physical reference; alignment permission is ephemeral and must be renewed after interruption/relaunch. Persisting a permission flag cannot establish the actual physical pose after the app closes. Restore must always open comparison, never trust a saved aligned flag. Alignment remains guarded before initial preview.

T05 remains running. Required next work: bounded versioned checksummed storage with replay and pose/index checks, actual atomic filesystem failure tests, restore, deletion generations, remaining completion/recovery states and app integration. No user-facing guide or physical qualification is claimed by this increment.

Increment 2 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-protocol-pr Scripts/test-pr.sh` exited 0. All 94 Swift package tests (30 session tests), 34 Python tests and three simulator app/UI tests passed, with zero skipped/failed. The unchanged solver passed the 10,000 unique-state corpus, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes matched the captured test inputs after execution. Portable red/green logs, package output, source identity and simulator summary are under `t05/protocol/`; full local artifacts and `.xcresult` remain under `Artifacts/t05-protocol-pr/`.

## Increment 3 — bounded guide archive and restoration

GuideArchive version 1 stores an explicit schema, base64 JSON payload and SHA-256 of the exact payload bytes. The entire encoded file is capped at 256 KiB before decode. Unknown versions are identified before interpreting version-specific payload fields; filesystem preservation of unknown files remains the store's next obligation. The checksum detects corruption, not authenticity.

The payload preserves the original canonical state, bounded move notation, original hash, resource version, explicit semantic palette, save ID, acknowledged action count, move index, pose, acknowledged state, pending action ID and preparation marker. Decoding validates legality, independently replays the full plan through Replay, reconstructs the deterministic action timeline and requires exact agreement with saved state/pose/index/identity. A checksummed payload cannot bypass those semantic checks. Resource versions are nonempty and capped at 256 UTF-8 bytes; sequence zero and a prepared action after completion are rejected.

Restoring into Session opens resumeCheck, or expectedSolved when all actions are durable. It retains the write sequence, has no pending write/playback, and resets alignment permission. The caller retains the restored explicit palette separately from the mathematical Session. No camera swatches, draft files or completion-origin persistence is claimed here; those remain T05 integration work.

TDD: roundtrip first failed behaviorally on the callable empty codec; envelope and re-signed semantic mutation tests then exposed missing validation guards; restore first failed with an empty Session. Focused GREEN passed all 34 current session tests. Cases include each acknowledgement boundary of a regrip+turn plan, prepared/unprepared states, malformed/truncated input, oversized input, unknown schema, wrong checksum, wrong original hash, resource limits, wrong pose/state/index/action identity, invalid original cube and wrong/overlong plans. Logs: `Artifacts/t05-archive-{roundtrip,guards,restore}-red.log` and `Artifacts/t05-archive-session-green.log`.

T05 remains running. This codec/restoration increment does not establish atomic file replacement, low-storage/locked-file behavior, delete ordering or iOS file protection. Those need the actual store and fault tests before the task's exit.

Increment 3 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-archive-pr Scripts/test-pr.sh` exited 0. All 98 Swift package tests, 34 Python tests and three simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the captured test inputs. Portable evidence is in `t05/archive/`; full logs and `.xcresult` remain in `Artifacts/t05-archive-pr/`. No filesystem durability or complete-product qualification is inferred from this result.

## Increment 4 — actor-owned guide filesystem store

GuideStore owns one dedicated directory through one actor instance. It reads at most 256 KiB + one byte and delegates all archive validation to GuideArchive. Missing files return no saved guide; malformed/oversized/unsupported files throw and survive attempted saves until explicit deletion. Production composition must own exactly one instance for the directory; independent stores in tests represent sequential process reopen, not supported simultaneous writers.

Saves write a same-directory `guide.pending`, synchronize and close its file handle, atomically rename to `guide.json`, then synchronize the directory. A pending file is never promoted to acknowledged progress on load. Later saves replace abandoned pending bytes; deletion removes both files. Backup exclusion is set on the owned directory. iOS file protection, Application Support path composition and actual locked-phone behavior remain required adapter work; this increment does not claim them.

An opaque lease identifies the current store generation. Deletion invalidates it before touching files, runs behind an in-flight synchronous actor write, and returns success only after removals/directory synchronization succeed. Even failed deletion rejects old producers. Save ordering rejects older revisions/sequences and conflicting payloads at an already-durable ID. A same-ID exact snapshot may be safely rewritten. Global deletion still needs orchestration across draft/preferences/diagnostics and the Session reducer; this store currently owns only the guide archive.

TDD: real file roundtrip failed on the callable store boundary; deletion/preservation tests exposed missing invalidation and unwanted overwrite; boundary tests exposed missing checkpoint propagation; ordering tests demonstrated a delayed old request replacing newer progress before the guard was added; backup exclusion failed behaviorally before setting the resource value. The first concurrency-test draft used a semaphore API directly in an async closure and failed compilation; corrected with a synchronous blocking helper before the behavioral run. Compilation failure is not counted as RED evidence.

Focused GREEN: all 43 session tests passed. Filesystem cases use real temporary directories, reopen the store, inject throws before writing/after temporary synchronization/before and after replacement, test a real OS write failure when the pending destination is a directory, preserve unreadable files, ignore abandoned partial bytes, inspect the backup-exclusion flag, and block an in-flight replacement while deletion is requested. Every injected write failure is reported; recovery sees the complete old archive before replacement or complete new archive after it. A callback failure after replacement is ambiguous physical progress and must enter comparison, never blind replay.

These are desktop filesystem and injected-boundary tests, not process-kill/power-loss, real disk-full or locked-iPhone qualification. No unexpected-file deletion, raw-frame persistence or product upload is introduced. Full T05 exit remains open: file protection/app composition, full draft/display persistence, all completion/recovery/deletion states, mutation checks and the remaining physical/fault evidence.

Increment 4 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-store-pr Scripts/test-pr.sh` exited 0. All 107 Swift package tests, 34 Python tests and three simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/store/`; full local logs and `.xcresult` remain in `Artifacts/t05-store-pr/`. T05 and the full product/release goal remain incomplete.

## Increment 5 — app storage composition, protection policy and denied reads

The app target now links CubeSession. AppDependencies owns a GuideStore rooted under Application Support/CubeGuide/Guide; tests supply an isolated child directory. The simulator integration exercises the real solver→reducer→concrete save→archive restore→physical comparison path and deletion through that app dependency. It does not implement the guide UI or global deletion of other local data.

On iOS, the store requests complete file protection for the guide directory and temporary file before writing payload bytes. Protection-setting failure propagates before archive replacement. A test-only injected protection boundary demonstrates directory and temporary-file failures leave no committed archive and no payload bytes. This is adapter/error-ordering evidence; a successful Foundation setter does not establish actual encryption or locked-device behavior. Apple's [file encryption guidance](https://developer.apple.com/documentation/uikit/encrypting-your-app-s-files) describes the platform's protection levels; actual physical passcode/lock testing remains mandatory.

Observed limitation: iOS 18.5 simulator attribute dictionaries omit NSFileProtectionKey, both before and after a successful setter. The initial assertion also attempted an unsuitable typed cast; correcting it to the raw string did not make the attribute appear. The complete attribute dictionary is retained in `Artifacts/t05-protection-observation.log`. These failures are not counted as valid behavioral RED/GREEN proof of file protection. Physical-device builds retain the protection assertions; simulator builds explicitly exercise save/restore and backup exclusion without claiming protection evidence. Physical protection/locked-file qualification remains open, not passed or waived. A first setter call had the wrong argument label; corrected compilation is not behavioral TDD evidence.

A separate real desktop permission-denial test did expose a product defect: fileExists returned false for an inaccessible directory and load reported an absent session. Load now opens directly and returns nil only for ENOENT; other OS errors propagate. The behavioral red transcript is `Artifacts/t05-store-permission-red.log`. With directory permissions removed, load/save/delete report errors; after permission restoration, the original archive remains readable. This is not a substitute for testing a locked iPhone.

Protection error-ordering RED: `Artifacts/t05-protection-failure-red.log` (the test description initially said V18; corrected to V15, the existing privacy suite). Focused GREEN: all 45 session tests passed in `Artifacts/t05-protection-package-green.log`. Full product/UI, draft/preferences/diagnostics lifecycle, completion states, force-termination, physical protection/low-storage and T05 mutation gates remain outstanding.

Increment 5 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-protection-pr Scripts/test-pr.sh` exited 0. All 109 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. Simulator protection assertions are explicitly outside this result as described above; actual physical qualification remains notRun. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/protection/`; full local logs and `.xcresult` remain in `Artifacts/t05-protection-pr/`. T05 and the full product/release goal remain incomplete.

## Increment 6 — bounded manual draft model

ManualDraft retains semantic sticker colors separately from canonical mathematical labels. Construction requires a validated explicit CenterPalette, writes only its six centers and leaves all 48 other stickers empty. Immutable edits validate row/column bounds before arithmetic, permit clearing a sticker, reject direct center edits, and increment a checked revision. Center reassignment changes the six centers while preserving entered semantic sticker colors. Face rotation corrects the chosen nine-cell layout, including empty cells; it is not a physical layer move and leaves all other faces unchanged.

A complete draft converts colors through its explicit palette to Facelets. It does not infer missing colors, repair counts or declare legality; the existing CubeValidation remains responsible for classification. Partial drafts report incomplete. Codable preserves partial input and revisions, reads at most 54 cells, and rejects malformed shapes, invalid color names/revisions and center disagreement. Whole-file bounds/checksum/version and the actual draft file store remain the next persistence increment; this model alone is not a saved-draft claim.

Behavioral RED/GREEN: explicit centers/empty cells, edits/revisions, center reassignment, clockwise face correction and canonical conversion, then decoding shape/center guards. The first rotation-test draft omitted `try` in one assertion and did not compile; corrected before the actual behavioral RED run, not counted as TDD evidence. Expected rotation is a literal nine-cell permutation checked on all six faces. Conversion uses the independent literal R fixture with a nondefault center arrangement, plus solved and deliberately invalid input to prove no guessed correction. The final focused run passed all 51 session tests.

Logs: `Artifacts/t05-draft-edits-red.log`, `Artifacts/t05-draft-conversion-behavioral-red.log`, `Artifacts/t05-draft-decode-red.log`, and `Artifacts/t05-draft-session-green.log`. Session reducer/editor integration, durable drafts, scanned measurement/crop metadata and sampled display swatches remain outstanding. T05 is still running; no manual-entry UI is claimed.

Increment 6 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-draft-pr Scripts/test-pr.sh` exited 0. All 115 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/draft/`; full local logs and `.xcresult` remain in `Artifacts/t05-draft-pr/`. Physical/media/distribution qualification remains open; T05 and the full product goal remain incomplete.

## Increment 7 — durable draft archive and shared session store

DraftArchive uses the same bounded version-1 checksum envelope as guide archives, now extracted to CheckedArchive. Payload validation remains separate: ManualDraft enforces shape/centers and preserves partial input; GuideArchive still independently replays and reconstructs progress. Existing guide envelope behavior and bytes retain the same schema. No raw images or inferred sticker corrections enter either archive.

GuideStore is renamed SessionStore, with SessionStoreError; app composition now exposes `sessionStore`. All source/test consumers are updated. The directory path remains Application Support/CubeGuide/Guide to preserve the existing location; it now contains `guide.json` and `draft.json`, each with its own `.pending` file. One actor, lease and deletion operation own both. Reading and protected atomic replacement share one implementation. No concurrent independent store instances may own the same directory.

Draft writes require the current lease and reject older or conflicting equal revisions. Identical retries are safe. Corrupt/unsupported existing drafts are preserved until explicit discard; guide data remains unaffected by draft errors. Global store deletion invalidates both producers and removes both committed and pending files, without claiming deletion of the still-unimplemented preferences/diagnostics stores. New/replacement editor flows must continue from the restored revision or explicitly discard old work; they cannot reset revision zero over a newer durable draft.

Behavioral RED/GREEN observed for draft envelope roundtrip, real filesystem draft+guide coexistence, stale/conflicting draft writes, unknown-draft preservation and combined deletion. Existing guide tests protect the archive/file-operation refactors. Additional draft boundary tests reuse the established failure seam, checking before-write, temporary-synced, before-replace and after-replace outcomes while preserving an independently stored guide. These added boundary cases passed on the shared implementation and are not misreported as a new RED cycle. Focused GREEN: all 57 session tests passed. App integration now also writes/restores an incomplete draft and verifies both files disappear after deletion.

Portable logs under `t05/draft-store/`; full local transcripts start `Artifacts/t05-draft-{archive,store}-`. Reducer/editor save orchestration, replacement consent, complete workflow/deletion matrix, scan metadata/display swatches, process termination and physical qualification remain outstanding. T05 remains running; no user-facing manual editor is claimed.

Increment 7 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-draft-store-pr Scripts/test-pr.sh` exited 0. All 121 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/draft-store/`; full local logs and `.xcresult` remain in `Artifacts/t05-draft-store-pr/`. Physical/media/distribution gates and the full product goal remain open.

## Increment 8 — draft reducer and durable validation barrier

Session now owns the current ManualDraft, last durable draft and pending DraftSaveRequest. DraftEdit covers explicit centers, a bounded sticker edit and face rotation. An accepted edit increments the session revision, clears prior confirmation and emits a concrete save command; the reducer enters savingDraft. Only the matching draftPersisted event commits the durable draft and returns to editing. Wrong/old callbacks are ignored. Further edits and validation are blocked while saving, so a later editor cannot race ahead of its save acknowledgement.

validateDraft requires the exact durable model and normalizes through its explicit centers before existing classification. When a draft exists, the older validate(Facelets) entry also rejects a different supplied cube; callers cannot bypass the reviewed colors. The legacy facelet entry remains available for the separately planned scan-review adapter. Restoring a partial draft opens editing without claiming it was confirmed. Subsequent edits rebase their draft revision to the current session revision, including after validation/edit navigation.

Cancellation during a draft write retains savingDraft and returns Home only on matching success. Failure preserves the entered draft, enters draftStorageError and requires a new save attempt with a new ID. Backgrounding does not discard the outstanding draft write. A transient discard-only-in-memory draft path was removed during review before this increment was committed: the file may already have been replaced when a callback reports failure, so that path would falsely imply durable discard. Explicit durable discard/global replacement remains unimplemented and required; it is not silently replaced by memory-only rollback.

The specification's workflow is refined with two explicit persistence substates, savingDraft and draftStorageError. Existing matrix coverage expanded to 16 phases × ten original events, plus 21 phase/substate fixtures × eleven guide-persistence events. The new draft matrix covers all 16 phases plus partial/complete editor variants across seven edit/save events (18×7). Rejected events preserve state and emit no command. Full final scanning/completion/deletion coverage remains required at T05 exit.

TDD: concrete edit/save, failed-save retry and interruption/restore tests failed behaviorally against callable declarations before implementation. Additional integration/guard tests execute 49 real draft saves, real validation/search, independent replay and real guide save; they verify the existing behaviors rather than claim separate RED cycles. All 63 session tests passed in the focused run. Tests also cover stale save IDs, incomplete drafts, mismatched external validation, invalid cells, revision exhaustion and revision continuity after validation/edit navigation. Logs: `Artifacts/t05-draft-reducer-{red,final-green}.log`.

Remaining T05 work includes actual app command orchestration and restore selection across guide/draft, durable replacement/discard and global delete state transitions, completion/recovery/scan states, scan metadata/display mapping, mutation checks and physical/process-termination evidence. The user-facing editor is still T06; this increment does not claim it exists.

Increment 8 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-draft-reducer-pr Scripts/test-pr.sh` exited 0. All 127 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/draft-reducer/`; full local logs and `.xcresult` remain in `Artifacts/t05-draft-reducer-pr/`. T05 and all outstanding physical/media/distribution gates remain open.

## Increment 9 — deletion state and callback isolation

The reducer exposes confirmed deleteLocalData, retryDeletion and matching success/failure events. A new DeletionID identifies each attempt independently of save counters. Deletion increments the input revision where possible, emits cancellation for search and preview plus the deletion command, clears pending write/playback callback identities and enters deleting. It retains the prior model until the operation succeeds; only matching deleted returns an empty Home state. Repeated delete while pending and stale callbacks are ignored. Failure enters deletionError, retains context and requires a new identified retry, without claiming erasure or restarting playback.

The protocol works from every currently implemented phase, including an in-flight guide/draft save. The underlying SessionStore serializes deletion behind writes and invalidates storage leases; integration tests execute that real store operation before sending success. The command name covers the eventual full local-data operation, but current concrete storage owns only guide/draft. Settings/diagnostics reset and camera cancellation must be added to application orchestration when those features exist; this increment does not certify full R18 deletion.

Deletion remains possible at UInt64.max without wrapping the session revision. Subsequent new-work events still return the existing revisionExhausted error; a fresh process can start normally after files are deleted. UI handling for that explicit exceptional outcome remains required. No counter exhaustion can prevent the file-erasure command itself.

Behavioral RED: callable event declarations failed confirmation, barrier, retry and late-producer tests in `Artifacts/t05-deletion-red.log`. Focused GREEN: all 69 session tests passed in `Artifacts/t05-deletion-final-green.log`. Expanded prior matrices cover 18 phases × ten original workflow events, 23 guide phase/substate fixtures × eleven events and 20 draft phase/substate fixtures × seven events. A new table covers all 18 phases × five deletion events. Additional tests preserve guide context after failure, ignore stale playback/write callbacks, execute real guide+draft deletion/reopen, reject old leases and exercise revision exhaustion. Those additional cases extend already implemented behavior and are not reported as separate RED cycles.

Remaining T05 work: app command execution/restore precedence, durable replacement and draft discard, completion/recovery/scanning states, scan/display metadata, mutation checks and required physical/process-termination evidence. The user-facing deletion confirmation/error controls remain part of the forthcoming Settings/UI integration. Nothing is publicly shipped.

Increment 9 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-deletion-pr Scripts/test-pr.sh` exited 0. All 133 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/deletion/`; full local logs and `.xcresult` remain in `Artifacts/t05-deletion-pr/`. T05 and all outstanding physical/media/distribution gates remain open.

## Increment 10 — consistent relaunch selection

SessionStore.restore reads both bounded archives and captures the storage lease within one actor operation without suspension. It returns SessionRestoration containing a reconstructed Session, its explicit palette and that lease. Empty storage returns Home; a sole/newer draft returns unconfirmed editing; a sole/newer guide returns physical comparison, or expectedSolved for fully acknowledged progress. Alignment/playback never resumes automatically. The higher input revision determines precedence, not file modification time. All adapters must continue using the shared reducer revision when accepting replacement input.

Equal revisions are accepted only when the complete draft normalizes to the guide's original state and its explicit palette agrees. Otherwise conflictingRecords is reported. Corrupt/unsupported archives propagate an error rather than silently disappearing behind another record; restore performs no writes. A returned lease becomes stale on deletion, preventing an old snapshot from saving deleted work. The eventual UI command executor must additionally reject delayed restore results after deletion before displaying them; that orchestration remains pending.

AppDependencies now exposes restoreSession. The simulator composition test exercises draft-only restoration, then restoration of a higher-revision guide, then empty Home after deletion. Its previously arbitrary draft revision was adjusted to precede the guide's revision to model that actual sequence; a separate package test explicitly exercises the opposite ordering. There is no release UI yet.

TDD: callable restore first returned empty Home and failed draft/guide selection tests; equal-revision conflicts then failed before their guard was implemented. An initial test omitted await for the actor's currentLease and failed compilation; corrected before the behavioral RED run. Additional corruption and stale-lease cases passed on the shared validated loader/generation implementation and are not counted as separate RED cycles. Focused GREEN: all 75 session tests passed. Logs: `Artifacts/t05-restoration-behavioral-red.log`, `Artifacts/t05-restoration-conflict-red.log` and `Artifacts/t05-restoration-final-green.log`.

T05 remains running. App side-effect execution, durable replacement/discard, completion/recovery/scanning states, camera/display metadata, mutation tests and outstanding physical/process-termination gates are still required. No simulator evidence proves physical alignment or file protection.

Increment 10 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-restoration-pr Scripts/test-pr.sh` exited 0. All 139 Swift package tests, 34 Python tests and four simulator app/UI tests passed, with no skipped/failed tests. The unchanged solver passed all 10,000 PR states, 46,741 shallow states, named cases and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is in `t05/restoration/`; full local logs and `.xcresult` remain in `Artifacts/t05-restoration-pr/`. No full-product or physical/media/distribution qualification is claimed.

## Increment 11 — observable session command coordinator

SessionController is a MainActor observable owner for the existing reducer. It restores validated storage before accepting normal workflow input, publishes the session/load status/error, and executes concrete solver, draft, guide and deletion commands. AppDependencies composes it with the real SolverService and SessionStore. The renderer is a required GuidePlayback dependency; there is no production no-op renderer or synthesized preview completion. Actual SwiftUI workflow and rendering remain T06/T07, so this does not claim a finished app screen.

Storage commands are queued in reducer order. Each write captures its storage lease and explicit palette; only actual success sends the matching reducer acknowledgement. An error is exposed only for an accepted callback. Deletion immediately invalidates delayed restore/write/solve/preview delivery, cancels the search task and stops playback, then waits behind outstanding storage operations. It gets the current lease for deletion (including a retry after a failed attempt rotated it) and returns Home only after success. The store still owns only guide/draft; future camera/preferences/diagnostics producers remain to integrate.

The coordinator passes the actual budget and revision to SolverService, whose worker runs off MainActor. Task cancellation reaches the service's cancellation handler, including when delivery is delayed. Preview commands share GuideAction and PlaybackID with the reducer; finishing playback never acknowledges a physical move. Restore requires comparison, pause/replay use new callback identities, and background stops playback. Starting replacement entry clears old display centers; missing explicit centers fails guide persistence rather than inventing a palette.

Behavioral RED/GREEN: callable coordinator declarations failed real edit/save/consent/guide/deletion and corrupt-restore tests before implementation. Initial test harness compile errors (missing try and wrong Facelets member name) were corrected before behavioral RED. A stale generated `.cstemp` inside the package build directory caused a signing failure; only that generated file was removed and the same test rerun. Neither compilation nor signing failure is counted as TDD evidence. A separate failing replacement-palette assertion led to clearing old centers. The playback test originally asserted an absolute stop count despite replay already emitting stop; corrected to assert the background increment.

Additional tests use explicit continuation gates, not scheduling sleeps, to hold a real saved draft, restored snapshot or verified solver result. They cover delete while writing, late restore after delete, late verified solve after cancellation, failures after actual draft/guide replacement, ambiguous physical acknowledgement, deletion failure after lease rotation, and new input after deletion. These passed the coordinator implementation and are regression evidence, not separate claimed RED cycles. The simulator app composition test exercises real draft-save/delete commands; its bounded wait observes state changes and makes no latency claim. No physical crash/locked-phone/performance or renderer proof is inferred.

T05 remains incomplete: durable replacement/discard, remaining completion/recovery/scanning states, scan/display metadata, complete mutation qualification and process/physical gates remain open. The app still needs its user-facing workflow. No public shipment or distribution has occurred.

Increment 11 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-controller-pr Scripts/test-pr.sh` exited 0. All 148 Swift package tests (including 84 session tests), 34 Python tests and five simulator app/UI tests passed, with no skipped/failed simulator tests. The unchanged solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable RED/GREEN logs, source snapshot, toolchain and simulator summary are under `t05/controller/`; full local logs and `.xcresult` remain under `Artifacts/t05-controller-pr/`. T05 and the full product goal remain incomplete.

## Increment 12 — durable manual-entry and replacement boundary

Starting manual entry now emits a concrete start request and enters startingManual. Ordinary editing/validation cannot begin until matching durable success. Existing guide/draft context remains in memory while replacement is pending or failed; it is not treated as usable guidance. The reducer clears old pending save/playback identities immediately. A failed start enters manualStartError and requires a new identified retry or confirmed global deletion; it cannot pretend that cancelling in memory undid a possibly committed file replacement. Cancelling while pending returns Home only after success, with the new empty work available to Resume. Background leaves the write running. Deletion invalidates pending start callbacks.

SessionStore owns two additional files, manual-start.json and manual-start.pending. A bounded, checksum-protected version-1 record stores a positive input revision without inventing center colors. A new boundary must exceed every existing input revision. Restore selects only draft/guide records newer than that boundary, otherwise returning an empty, unconfirmed editor at its saved revision. Writes at or below the boundary are rejected, so old producers cannot resurrect the replaced guide. Existing installations without the record retain their prior restore behavior.

Identical start retries repeat the protected atomic write and synchronization, even when the marker is already visible: a prior callback may have failed after rename. Rewriting the same boundary preserves newer draft input. Older guide/draft files remain bounded stored records until overwritten or globally deleted; they are not an undo/history feature and restore cannot select them. Low-level archive loaders still expose raw records for validation/tests; application composition uses restore for active-work selection. Corrupt/unsupported records continue to surface errors without silent overwrites. Global deletion removes all six committed/temporary paths, including the new boundary.

SessionController executes the new request on its existing ordered storage queue and updates display centers only after accepted success. Continuation-gated integration tests exercise replacement of a real guide, an error after the boundary was written, retry, relaunch before center assignment, subsequent draft input and deletion while the start write is outstanding. App simulator composition now waits for the real start save before accepting centers. Pure reducer fixture helpers explicitly acknowledge their fake start save; this is not substituted for the real store/controller integration tests.

TDD: store replacement/atomic-boundary tests failed against a callable no-op; reducer barrier/interruption tests failed before implementation; a retry fault test failed because an identical boundary incorrectly skipped the durability path. These are recorded in `Artifacts/t05-manual-start-{store-behavioral,reducer,retry}-red.log`. The initial store test referred to a private failure enum from another file and failed compilation; corrected before behavioral RED, not counted as a behavior failure. Additional archive/order/generation/matrix cases extend the regression coverage without claiming separate RED cycles.

Current transition tables cover all 20 implemented phases: original workflow 20×10; guide 25 phase/substate rows×11; draft 22×7; deletion 20×5; new start callbacks/retry 20×3. Confirmed replacement is also tested with actual old work and pending guide callbacks. These are not the final scanning/completion matrices. T05 still requires remaining completion/recovery/scanning states, explicit durable draft discard, camera/display metadata, full mutation qualification and outstanding process/physical checks. App UI, media and distribution remain unfinished; nothing has publicly shipped.

Increment 12 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-manual-start-pr Scripts/test-pr.sh` exited 0. All 158 Swift package tests (94 session tests), 34 Python tests and five simulator app/UI tests passed. No simulator test failed or skipped. The unchanged solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is under `t05/manual-start/`; full logs and `.xcresult` are under `Artifacts/t05-manual-start-pr/`. T05 and the full product goal remain incomplete; physical/media/distribution gates remain open.

## Increment 13 — actual host process-termination qualification

Added a development-only SessionStoreCrashProbe executable and Tools/StorageCrash harness. The helper creates the fixed literal R guide through the real reducer and independent runtime replay, then exercises the real protected/atomic SessionStore implementation. StoreBoundary now has raw string identifiers and package access, and the existing fault-checkpoint initializer has package access so this executable can select a stop point. Neither is a new public app API; the helper is not an app product dependency.

The parent observes the child's actual SIGSTOP status through waitpid, then sends SIGKILL and requires that termination signal. It opens the directory with a fresh probe process, verifies the active snapshot, and retries the interrupted operation. Normal exit, an incorrect boundary announcement, unexpected signal or timeout fails qualification; the parent kills/reaps children on failure. Independently specified seed/operation snapshots prevent a helper that performs the wrong mutation from passing merely because two runs agree.

All 13 cases passed in `Artifacts/t05-crash-integrated/storage-crashes/report.json`: guide acknowledgement, draft replacement and manual-entry replacement at beforeWrite, temporarySynced, beforeReplace and afterReplace, plus deletion at beforeDelete. Before replacement the previous complete state remained active; after replacement the new complete state was active. Restored alignment stayed false. Retrying reached the complete expected state despite abandoned temporary files. Per-case directories retain the exact files at interruption, their hashes, before/expected/recovered/retried snapshots and the child's signal evidence. The report records host, Python, probe-binary hash and harness-source hash.

TDD: the first callable mutate command did nothing, and the real-process qualification rejected all 13 cases because durable state did not change (`Artifacts/t05-crash-red/`). After implementing actual operations and stop points, all cases passed. Two additional fixture-validation tests failed on a wrong operation and an already-advanced seed; the literal snapshot oracle then made them pass. Six Python harness tests now pass; they include real child stop/kill, normal-exit rejection, wrong-boundary rejection and timeout cleanup/reaping. The fixture checks use declared snapshots; they are guard tests, not substitutes for the 13 Swift-store process cases.

Review of the first fixture identified that its guide acknowledgement covered the initial regrip only. Four final face-turn cases were added, starting from the durably prepared final turn and expecting expectedSolved after its acknowledgement becomes durable. The extended harness failed all four against the old seed (`Artifacts/t05-crash-turn-red/`), while the original 13 still passed. After extending the real reducer fixture, all 17 passed in `Artifacts/t05-crash-turn-green/`. This checks host file restoration around both regrip and final-turn progress; it is not evidence of a human performing those actions on a physical cube.

`Scripts/test-storage-crashes.sh` builds the helper, runs those guard tests and retains qualification output. `Scripts/test-pr.sh` invokes it after package tests. Instructions and scope are in `Tools/StorageCrash/README.md`. The harness rejects existing qualification output directories before creating cases. This is stronger than injected Swift errors but still only proves macOS host process termination at the named boundaries. It does not prove power-loss durability, interrupted device writes, locked-phone behavior, disk-full behavior, an installed iOS app kill, or interruption between individual deletion removals. Those gates remain open, along with remaining T05 workflow/model work and the later UI/media/device/distribution tasks.

Increment 13 final regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-process-crash-final-pr Scripts/test-pr.sh` exited 0. All 158 Swift package tests, 40 Python tests, 17 actual process-kill cases and five simulator app/UI tests passed. No simulator test failed or skipped. The unchanged solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. The rebuilt app bundle and project dependencies contain no SessionStoreCrashProbe executable/dependency. Portable reports, RED evidence, logs, source state and toolchain are under `t05/process-crash/`; raw interrupted files and `.xcresult` remain under `Artifacts/t05-process-crash-final-pr/`. The earlier full run with 13 cases also passed before the final-turn fixture expansion; it is not used to claim the final 17-case coverage. T05 and the full product goal remain incomplete; no public shipment occurred.

## Increment 14 — explicit durable completion evidence

Session adds savingCompletion, completionStorageError and completed. confirmCompletion is accepted only for already-solved confirmed input or fully acknowledged guidance. It produces a concrete guide-archive save; only its matching success publishes completion. CompletionKind distinguishes enteredColorsSolved from userConfirmed. No camera-verified enum value or claim is available through this path. An already-solved manual state uses an independently replayed zero-move plan with resource version `entered-colors-v1`, without issuing a solver command or fabricating physical moves. This records the entered-state result, not a camera observation.

The guide archive gains an optional completion field in its existing checked payload. Missing metadata retains legacy behavior: complete progress restores expectedSolved and requires confirmation. New metadata must match independently reconstructed complete progress. enteredColorsSolved requires a solved original and zero moves; userConfirmed requires nonempty completed guidance. Unknown kinds, premature completion, or an entered-color label on a scrambled original are rejected. This optional additive field leaves existing unconfirmed archive behavior intact. Identical save-ID retries now also require the same completion kind.

Failed completion saves retain the confirmation intent and candidate, expose completionStorageError and require explicit retry with a new save ID. Background allows the write to settle. Cancel while saving returns Home only after success. Deletion invalidates outstanding completion callbacks. A completed session can exit Home and Resume back to completed, preserving its evidence kind; it does not offer the original scramble for solving again. Restored completion still has no trusted physical alignment. Camera verification, mismatch/recovery transitions and the actual S10 UI remain outstanding.

TDD: confirmation/barrier, manual classification and failure/interruption tests failed before reducer implementation. Archive roundtrip, premature encode and same-ID identity tests failed before archive/store support. Initial payload-mutation tests accidentally used an unlabeled trailing closure that changed the envelope rather than payload; corrected to explicit `payload:` and not counted as valid semantic-mutation RED evidence. The corrected legacy test exposed a real issue: re-confirming a legacy empty plan selected userConfirmed and failed its own archive rule. It now retains enteredColorsSolved; `Artifacts/t05-completion-legacy-behavioral-red.log` records that failure. Unknown camera claims and re-signed invalid completion metadata are rejected in the final tests.

Additional tests exercise real file replacement at all four write boundaries, completion metadata after a reported post-write failure, retry, relaunch, deletion and a complete saved manual draft through SessionController. The simulator composition test uses the installed package/store and checks completion plus restoration; it is not a finished-screen UI test. Focused GREEN passed all 104 session tests. Matrices cover all 23 implemented phases: original workflow 23×10, guide 28 phase/substate rows×11, draft 25×7, deletion 23×5, manual-start callbacks 23×3, completion/retry 23×2. Scanning and the remaining recovery/practice states are not claimed covered.

T05 remains incomplete. Remaining work includes mismatch/recovery/scanning and verification contexts, durable draft discard, scan/display metadata and complete mutation qualification. Actual product UI, human media, physical-device/storage/camera/usability gates and distribution are still outstanding. No public shipment occurred.

Increment 14 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-completion-pr Scripts/test-pr.sh` exited 0. All 168 Swift package tests (104 session tests), 40 Python tests, 17 existing host process-kill cases and six simulator app/UI tests passed. No simulator test failed or skipped. The unchanged solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Completion-specific file boundaries were tested with injected failures; the existing process-kill corpus covers shared guide/draft/manual-start writes and pre-delete, not a new dedicated completion-metadata kill case. Portable evidence is in `t05/completion/`; full logs and `.xcresult` are in `Artifacts/t05-completion-pr/`. T05 and all outstanding physical/media/distribution gates remain open.

## Increment 15 — durable mismatch recovery

The mismatch event is now accepted from active guidance, expectedSolved and completed. It stops preview immediately, invalidates playback completion, clears the in-memory completion label, retains the verified model and emits a recovery save. Uncertain physical comparison uses the same durable path. savingRecovery blocks guidance while the write is pending; matching success enters recovery and failure enters recoveryStorageError. Retry uses a new save identity and never acknowledges a physical action. Background lets the write settle; cancel while pending reaches physical comparison only after success. Deletion invalidates late save callbacks.

GuideArchive adds optional recoveryRequired metadata. Legacy records default to false. A recovery record retains the original plan, acknowledged prefix and whether the pending action had been prepared, but cannot also carry completion metadata. Same-ID retry checks include the recovery flag. Restoration returns recovery instead of completed/guide comparison when the flag is set, and alignment is false. As with other atomic writes, failure before replacement may leave the prior complete record; failure after replacement may leave the new complete record. The controller does not claim durable recovery until success.

Cancelling saved recovery preserves the old model and enters resumeCheck, or expectedSolved after the final move. The recovery flag remains durable until a new explicit physical comparison or completion confirmation is saved. A before-match requires a fresh preparation save even if the action was prepared earlier; an after-match follows the normal acknowledgement barrier. These successful saves clear the recovery flag. Confirmed manual replacement is also allowed from recovery, through the existing durable manual-start boundary and a higher input revision. Without replacement consent it returns replacementRequired. The old guide is retained until that replacement succeeds. Camera/rescan and verification contexts remain separate unfinished work.

TDD: mismatch/uncertain-save tests failed behaviorally against callable declarations; archive/store tests failed because the old archive did not support a recovery record. The first harness build missed the new enum case because the edit did not match a formatted declaration; corrected before the behavioral RED run, not counted as a behavioral failure. Final tests cover preview callbacks after mismatch, cancellation, stale write IDs, real store retry after a post-write error, manual replacement, contradictory completion+recovery metadata and all four injected write boundaries. Existing recovery fixtures now explicitly acknowledge their fake recovery save; real filesystem/controller tests separately exercise the concrete operations.

Focused GREEN passed all 113 session tests. Matrices cover all 25 currently implemented phases: workflow 25×10, guide 30 phase/substate rows×11, draft 27×7, deletion 25×5, manual-start callbacks 25×3, completion 25×2 and mismatch/retry 25×2. The installed-app composition test now persists a mismatch after entered-color completion and restores recovery. It is not a finished S09/S10 UI or camera-flow test.

T05 remains incomplete: capture/review/paused-capture, recovery rescan and verification contexts, durable draft discard, scan/display metadata and full mutation qualification remain. Actual user-facing screens, human media, physical-device/storage/camera/usability gates and distribution remain open. No public shipment occurred.

Increment 15 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-recovery-pr Scripts/test-pr.sh` exited 0. All 177 Swift package tests (113 session tests), 40 Python tests, 17 existing host process-kill cases and six simulator app/UI tests passed. No simulator test failed or skipped. The unchanged solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Recovery-specific file boundaries were tested with injected failures; no new dedicated recovery-metadata process-kill case is claimed. Portable evidence is under `t05/recovery/`; complete logs and `.xcresult` are under `Artifacts/t05-recovery-pr/`. T05 and the full product goal remain incomplete; physical/media/distribution gates remain open.

## Increment 16 — recovery record under actual host process termination

Extended the real-process qualification corpus with mismatch recovery at beforeWrite, temporarySynced, beforeReplace and afterReplace. The probe builds the existing literal-R guide through the reducer, durably prepares the initial action, confirms alignment and emits mismatch. The actual SessionStore writes its recovery request. Inspection now exposes a true recoveryRequired flag, so the independent Python oracle requires both the recovery phase and durable metadata while preserving zero acknowledged actions and false restored alignment.

The parent retains its SIGSTOP/SIGKILL checks, fresh-process restore, exact snapshot comparisons and retry checks. Before replacement the prior resumeCheck record must survive; after replacement the recovery record must survive. This increment qualifies recovery from a prepared, incomplete guide. It does not claim process-kill coverage for clearing a previous completion label, camera recovery, power loss or physical iOS termination.

Test development first rejected the unsupported recovery operation. After adding the actual reducer/store operation, all four new cases failed the operation contract because inspection omitted the durable recovery flag (`Artifacts/t05-recovery-kill-contract-red/`). Exposing that metadata makes the oracle able to verify the required record rather than phase alone. No production reducer or storage behavior changed in this increment.

Increment 16 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-recovery-kill-pr Scripts/test-pr.sh` exited 0. All 177 Swift package tests, 40 Python tests, 21 host process-kill cases and six simulator app/UI tests passed. No simulator tests failed or skipped. The solver passed the 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable evidence is under `t05/recovery-process-crash/`; full interrupted files, logs and simulator result bundle remain under `Artifacts/t05-recovery-kill-pr/`. T05, product UI, media, physical qualification and distribution remain incomplete. Nothing publicly shipped.

## Increment 17 — bounded accepted scan observations

Added the planned CubeScan module with immutable ScanDraft, ScanFace, ColorMeasurement, DisplaySRGB, LabColor and CaptureMetadata values. CubeSession now imports CubeScan and preserves its existing public color/palette names through aliases. This shares one explicit semantic palette without a dependency cycle or a manufacturer-default assignment. Existing manual and guide archive representations remain unchanged.

Accepted faces occupy canonical URFDLB slots but arrive in F,R,B,L,U,D order. Replacing an accepted face requires an explicit flag; a duplicate capture cannot silently replace it. Every accepted observation/correction increments the revision with overflow rejection. Manual sticker overrides remain separate from the measurements, centers have a separate assignment operation, and duplicate center names are rejected. The complete center palette is unavailable until all six accepted faces have distinct confirmed names. Rotation correction permutes measurements and overrides together, records quarter turns, and preserves capture metadata and other faces.

Measurements retain finite D65 Lab values, a bounded tagged display-sRGB swatch, nonnegative finite spread and a usable sample count within the specified 40×40-cell maximum. Metadata retains processing dimensions limited to 1920, recorded source orientation/mirroring, four normalized upright-image corners, the exact canonical front/top cube pose and a bounded sampling version. Crop checks reject non-finite/out-of-image points, repeated/reversed/crossed/concave/collinear and numerically degenerate corners. The 1e-8 normalized cross-product guard is a numerical geometry constraint, not a calibrated confidence threshold. The model does not claim to generate these measurements from camera pixels yet.

Custom decoders enforce the same invariants and read at most six slots, nine measurements/overrides and four corners before rejecting extra values. Saved values cannot bypass shape, pose, center, crop or numeric checks. There is no raw-image field, guessed classification, solver-based correction, canonical facelet conversion or scan-verified completion constructor in this increment. Versioned archive envelopes and SessionStore/reducer integration for scans remain unfinished.

TDD: ten tests were written against callable constructors and no-op draft operations; the initial run recorded behavioral failures for missing validation and unchanged revisions/accepted faces (`Artifacts/t05-scan-draft-red.log`). Decode-fixture assertions were then changed from forced JSON casts to required values so malformed fixture setup reports an ordinary test failure. After implementation, the ten scan tests passed. The affected run also passed all 113 session tests, including existing archive/store/controller behavior after the shared-color move. Tests exhaust all 24 poses for each of six slots, literal clockwise rotation, empty/partial/full captures, explicit replacements, duplicate centers, numeric/crop/decode rejection and overflow. These use synthetic measurements and do not certify camera accuracy or physical orientation.

Compatibility check: the current probe read all 21 `files-at-interruption/` directories produced by the previous committed runtime in `Artifacts/t05-recovery-kill-pr/storage-crashes/`. Every restored snapshot exactly matched that run's recorded recovery snapshot, including the recovery flag. This reads actual archives created before the color-type relocation; it does not merely round-trip newly encoded values. The source report hash, current probe hash and case results are in `Artifacts/t05-scan-draft-legacy-archives.json`.

The first full PR run (`Artifacts/t05-scan-draft-pr/`) failed during the Xcode app-test build after package, Python, process-kill and solver checks passed. Xcode enables member-import visibility: using the relocated palette members through CubeSession aliases still requires importing their defining CubeScan module. Added the explicit import to app composition tests and declared CubeScan as an application package product/framework dependency. This was an integration defect, not a passing regression or a waived compiler diagnostic.

Increment 17 final regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-scan-draft-pr-final Scripts/test-pr.sh` exited 0. All 187 Swift package tests (including ten new scan tests and 113 session tests), 40 Python tests, 21 host process-kill cases and six simulator app/UI tests passed. No simulator tests failed or skipped. The solver passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable RED, focused/affected GREEN, failed/fixed Xcode integration logs, legacy-archive comparison, final source snapshot/toolchain/test summary are under `t05/scan-draft/`; full logs and result bundles remain under `Artifacts/`. T05 and the full product remain incomplete. No public shipment occurred.

## Increment 18 — one durable draft with retained scan context

The existing draft.json/draft.pending pair can now hold either a legacy manual draft or a tagged pending scan. There is still one real draft file and one active guide file, owned by the same SessionStore actor, protection/backup policy and deletion lease. Legacy manual payloads and their public archive APIs remain unchanged. A scan uses the same bounded version-1 checksum envelope with an explicit kind discriminator; unknown kinds, malformed contexts and invalid nested ScanDraft values fail decoding. No durable images were added.

PendingScan records the accepted observations, new-cube/recovery/verification purpose and the exact retained guide SaveID when applicable. Recovery and verification require that identity; its revision must precede the scan revision and its sequence must be positive. The store checks that it matches the actual active guide. Verification additionally requires complete guide progress. A scan save preserves the guide file byte-for-byte and cannot create a completion kind. Restore returns the validated pending scan alongside the retained session and lease, keeping physical alignment false. A scan is not silently selected as a replacement solver input.

Draft ordering now spans both kinds: older or same-revision conflicting manual/scan writes fail; an identical retry repeats atomic writing. Confirmed manual-start boundaries account for scan revisions, retire older scans and reject their late writes. A newer confirmed guide retires an older scan from active restoration; an equal-revision guide/scan conflict is rejected. While a scan is newer, a different retained-guide save identity cannot advance behind its back. Corrupt/future draft records block overwrites and remain available for explicit deletion. Global deletion already owns the same paths and rejects scan producers using the old lease.

The coordinator retains restored pendingScan instead of dropping it. Until the scan workflow resolves that context, manual/guide events cannot operate on its retained guide; confirmed deletion and its callbacks/retry remain available. This is an interim integration boundary: capture/review/pause/resume, explicit scan discard/replacement, scan-saving reducer commands and their UI still require implementation. No user-facing camera flow is claimed complete by exposing this restored value.

TDD: nine archive/store tests failed behaviorally against callable declarations/no-op storage, with 40 issues (`Artifacts/t05-scan-store-behavioral-red.log`). The initial harness build incorrectly combined two actor calls through a synchronous assertion autoclosure; fixed before that RED run and not counted as behavioral failure. After storage implementation, a new coordinator restoration test failed four assertions because it dropped the scan and allowed old guidance. Retaining the value and rejecting those unrelated commands made it pass. The affected suite passed all 124 session tests (11 new tests). File-fault coverage includes beforeWrite, temporarySynced, beforeReplace and afterReplace with reopen and retry, retained-guide checks and deletion. Dedicated scan process-kill cases remain unrun; the existing host corpus still covers the shared writer with guide/manual/recovery operations.

Increment 18 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-scan-store-pr Scripts/test-pr.sh` exited 0. All 198 Swift package tests, 40 Python tests, 21 existing host process-kill cases and seven simulator app/UI tests passed, with no failed/skipped simulator tests. The added app test persists/restores/deletes synthetic scan observations through the installed app's actual store and coordinator; it does not use camera capture. Solver checks passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. The new runtime also restored all 21 actual legacy interruption directories from `Artifacts/t05-recovery-kill-pr/storage-crashes/` to their recorded snapshots. Portable RED/GREEN logs, source/toolchain/test summary and that compatibility report are under `t05/scan-store/`; complete logs/result bundle remain under `Artifacts/t05-scan-store-pr/`. T05, scan workflow/UI, camera recognition, physical qualification, media and distribution remain incomplete. No public shipment occurred.

## Increment 19 — pure scan capture/review/save workflow

Added ScanWorkflow/ScanReducer with home, pausedCapture, scanning, freezing, faceReview, saving, storageError and editing phases. The reducer emits explicit start/stop/freeze/discard-frame and save commands; it does not import camera frameworks or store an image. An initial empty scan context is saved before camera start. A captured observation enters transient review without advancing the durable count. Explicit accept emits a save candidate and releases the review frame; only the matching save callback advances to the next named slot or six-face editing. Completion/legality/classification are not inferred from six accepted faces.

Capture and save identities include a fresh workflow UUID, the input revision and a checked sequence. Duplicate/stale callbacks cannot attach a frame or acknowledge a save in another workflow, after interruption, or after retry. A wrong-slot frame or capture failure pauses with a typed reason and preserves accepted observations. Resume requires explicit confirmation that the physical cube is unchanged. Interruptions/exit discard unaccepted review data and clear capture identity. An outstanding save settles before entering paused capture or Home; failure keeps its candidate and retry creates a new operation identity without changing the saved draft revision.

Review edits do not mutate the durable draft. Shared ScanFace operations now implement sticker override, center assignment and rotation for both unaccepted review and accepted ScanDraft corrections. Rotation keeps measurements and overrides together. Full-scan corrections have their own save barrier. Recapture explicitly selects an existing face, requires the physical check and replaces only that slot after save. Duplicate center names, invalid coordinates and counter/revision exhaustion produce typed rejection with unchanged state and no commands.

TDD: the first ten workflow tests failed behaviorally against the callable no-op reducer (`Artifacts/t05-scan-workflow-red.log`). After implementation, all ten passed. A follow-up lifecycle test exposed interruption during storageError being ignored: retry could start the camera without a physical check. It failed four assertions (`Artifacts/t05-scan-workflow-interruption-red.log`); handling interruption in both saving and storageError fixed it. The phase/event matrix includes all eight phases, two Home contexts and all 16 event kinds (144 combinations), with explicit accepted/ignored/rejected outcomes and no state/command changes for ignored/rejected events. The initial matrix's saving/accept entry was corrected to the documented duplicate-accept no-op before its first GREEN run. Other tests cover six ordered captures, literal rotation, retake, duplicate centers, wrong-slot/stale callbacks, save failure/retry, background/exit ordering, input/sequence overflow and a real SessionStore verification-context round trip.

This is the pure workflow layer. SessionController does not yet execute these new camera/save commands; it still retains pendingScan and blocks unrelated guide commands. Camera adapter integration, root navigation, initial-scan replacement authorization, scan discard/manual fallback, six-center classification, final review/validation and all physical camera tests remain outstanding. No completed camera or product UI flow is claimed.

Increment 19 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-scan-workflow-pr Scripts/test-pr.sh` exited 0. All 209 Swift package tests (135 session, ten scan, 27 core, 33 solver and four generator tests), 40 Python tests, 21 existing host process-kill cases and seven simulator app/UI tests passed. No simulator tests failed or skipped. Solver checks passed the 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable RED/GREEN logs and the final source/toolchain/simulator summary are under `t05/scan-workflow/`; full logs and result bundle remain under `Artifacts/t05-scan-workflow-pr/`. T05 and all unfinished coordinator/camera/UI/media/physical/distribution work remain open. No public shipment occurred.

## Increment 20 — scan coordinator commands and app composition

SessionController now owns the ScanWorkflow alongside its retained guide context and executes its save/camera commands. SessionStorage includes scan-draft saving on the same ordered queue as all other writes/deletion. The optional explicit ScanCamera adapter reports readiness, interruption and immutable observations; freeze is blocked until readiness. Camera start runs only after matching durable initial/accepted-face save. Stop cancels the current camera run; delivered review frames have an explicit discard command. The adapter contract requires late cancelled work not to retain new images. The concrete AVFoundation/processing implementation remains outstanding.

Async startScan validates the requested new-cube/recovery/verification context and replacement consent, then obtains a fresh validated store snapshot before choosing the next checked input revision and exact retained guide identity. This avoids relying on a potentially failed success callback. SessionRestoration now exposes that durable SaveID. A confirmed complete guide is rechecked against the fresh snapshot before verification starts. Missing camera, exhausted revision, unexpected pending scan or incompatible current state cannot create a new draft. Delayed startup reads are ignored after cancellation, backgrounding or confirmed deletion. Unrelated input cannot mutate the retained session during startup.

Restored scans initialize the reducer in pausedCapture (or complete-scan editing), with no automatic camera start. Existing background/cancel/resume events route to the scan workflow. Manual/guide commands remain blocked while it owns the pending input. Each camera activation has a fresh token in addition to the controller generation and reducer operation IDs; old readiness, interruption and frame callbacks cannot affect a resumed/new/deleted run. Confirmed deletion stops camera, discards its frame, invalidates callbacks, waits behind in-flight saves and clears scan state only after success. Deletion failures keep scan commands blocked until deletion retry completes.

TDD: the first test build hit a Swift 6.4 compiler assertion (`recordArgumentList`) when invoking a closure directly from #require. Splitting those expressions into explicit local variables resolved the compiler issue; it is not counted as behavioral RED. Eight coordinator tests then failed with 11 issues against the callable no-op methods (`Artifacts/t05-scan-coordinator-behavioral-red.log`). After implementation those eight passed. Recovery context and deletion-failure/late-frame cases expanded the affected suite to 145 session tests. A further fresh-state test failed two assertions because verification could enter a workflow before discovering that the durable guide was no longer complete; startup now rejects that case before creating the workflow (`Artifacts/t05-scan-coordinator-purpose-red.log`).

The tests use continuation-gated real storage and a controllable camera adapter. They cover save-before-camera ordering, readiness, all six accepted faces, no solver calls/no new completion claim, retry after a post-write failure, interruption, fresh guide identity, explicit replacement, missing camera, overflow, startup cancellation/deletion, recovery retention and late frames after failed/retried deletion and a new scan. The app dependency factory accepts the same camera interface. Its simulator composition test now drives start→fixture capture→review→accept→actual store→relaunch→delete through the coordinator, rather than writing scan records directly. Synthetic observations and fixture camera readiness are explicitly not real camera or image-recognition evidence.

Scan command execution is now wired. Root UI/navigation, AVFoundation and image processing, crop resampling, scan discard/manual fallback, classification and final reviewed-input acceptance remain unfinished. Physical qualification, human media and distribution gates remain open; no public shipment occurred.

Increment 20 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-scan-coordinator-pr Scripts/test-pr.sh` exited 0. All 220 Swift package tests (146 session tests), 40 Python tests, 21 existing host process-kill cases and seven simulator app/UI tests passed. No simulator tests failed or skipped. Solver checks passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable compiler/behavioral RED, focused/affected GREEN, final source/toolchain/simulator summary and package log are under `t05/scan-coordinator/`; full logs/result bundle remain under `Artifacts/t05-scan-coordinator-pr/`. T05 and the full product remain incomplete, including production camera/image processing, discard/manual fallback, final input acceptance, UI, media, physical qualification and distribution. No public shipment occurred.

## Increment 21 — durable draft discard with retained guide and revision ordering

Confirmed discard now replaces the shared manual/scan draft with a bounded, checksummed tagged record containing only a positive revision and optional retained-guide SaveID. The guide file is unchanged. Restore yields the retained guide with untrusted physical alignment, or empty Home when no guide remains, including after an empty manual-start boundary. A separate input revision floor preserves the highest durable revision while leaving existing guide/action/save identities intact. New manual entry and scan startup use that floor. Retained-guide acknowledgements remain allowed; old discarded-input plans, drafts and scan writes cannot replace the marker. Newer input/guide records supersede it normally.

The store rotates producer leases before the atomic discard write. Failure at each existing file boundary leaves a complete old or new record and rejects prior producers. Identical retries repeat synchronization; a stale retry cannot discard newer work. Marker decoding validates revision/guide metadata, and inconsistent retained-guide files fail restoration. Corrupt/unknown draft schemas survive selective discard attempts; confirmed global deletion remains their supported erasure path.

SessionController requires confirmation, exposes saving/failed status, stops capture/playback, cancels search and invalidates callback delivery immediately. It queues discard after earlier writes and returns to restored work only after success. A failed callback cannot be undone in memory; retry gets the current lease and repeats the durable operation. Global deletion can supersede pending/failed discard and waits behind its write. Camera callbacks from the discarded generation cannot attach observations to new input. The installed-app fixture composition now exercises discard and fresh restoration before global deletion; this is not a user-facing screen or live-camera test.

Behavioral RED: the initial three storage tests failed with 31 issues against callable no-op declarations (`Artifacts/t05-discard-store-red.log`). After implementation they passed. Two coordinator tests then failed with 11 issues against no-op methods (`Artifacts/t05-discard-controller-red.log`) before command execution was implemented. Eight new tests cover archive guards, guide retention and advancement, both manual states, all four write boundaries, stale leases/plans/retries, revision continuation, delayed draft writes, late camera callbacks, failed discard retry and superseding deletion. Focused/affected GREEN: all 154 session tests passed. Two additional assertions initially expected a redundant preparation save on before-comparison and revision zero in the same process after deletion; corrected against existing documented behavior to use after-comparison acknowledgement and the retained in-memory revision. These harness corrections are not counted as product RED fixes.

Dedicated discard process-kill cases, physical phone/locked/disk-full checks, complete T05 mutation qualification, scan manual fallback/final input acceptance, actual UI, production camera, media and distribution remain outstanding. Existing process-kill checks qualify shared writer boundaries, not a new dedicated discard-kill scenario. No public shipment is claimed.

Increment 21 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-discard-pr Scripts/test-pr.sh` exited 0. All 228 Swift package tests (154 session), 40 Python tests, 21 existing host process-kill cases and seven simulator app/UI tests passed. No simulator tests failed or skipped. Solver checks passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable RED/GREEN, corrected-harness failures, source/toolchain/test summary and package logs are under `t05/discard/`; full logs/result bundle remain under `Artifacts/t05-discard-pr/`. T05 and the full product remain incomplete; no public shipment occurred.

## Increment 22 — draft discard under actual host process termination

Extended the existing SIGSTOP/SIGKILL harness from 21 to 33 cases. Three additional fixed seeds exercise a pending recovery scan with a retained guide, a partial manual draft after confirmed replacement, and empty manual entry before center assignment. Each is interrupted at beforeWrite, temporarySynced, beforeReplace and afterReplace, then inspected from a fresh process and explicitly retried. All operations use the real SessionStore; the new scan fixture uses synthetic observations and makes no camera claim.

The independent Python snapshot oracle requires accepted scan metadata before discard, its absence afterward, the exact guide state or empty Home, false physical alignment, and revision floor six. It checks retained guide bytes remain unchanged. After retry, a separate process restores the session, invokes the actual reducer's manual-start event and writes its request; the resulting editor must be at revision seven. Thus a discard that loses its revision marker cannot pass merely because no draft appears. Added guard tests reject retained scan metadata, lost revision ordering, advanced/aligned guidance and reopening an abandoned empty editor.

Behavioral RED: with real fixed seeds and a callable no-op discard branch, all 12 new cases failed the expected-operation contract; the existing 21 passed (`Artifacts/t05-discard-kill-red/storage-crashes/report.json`). Wiring that branch to the actual store discard made all 33 pass (`Artifacts/t05-discard-kill-green/storage-crashes/report.json`). Eight Python harness tests passed. No production storage/controller behavior changed in this increment; it adds dedicated interruption evidence for the previously implemented discard path.

The probe remains a development-only executable and now declares CubeScan directly for its synthetic seed. This is host process termination at named boundaries, not power loss, locked-phone/low-disk qualification, installed-iOS termination, or interruption between individual deletion removals. T05 mutation qualification and remaining scan/manual/final-review integration, user-facing screens, production camera/media, physical testing and distribution remain open. Nothing has publicly shipped.

Increment 22 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-discard-kill-pr Scripts/test-pr.sh` exited 0. All 228 Swift package tests, 42 Python tests, 33 actual host process-kill cases and seven simulator app/UI tests passed, with no failed/skipped simulator tests. Solver checks passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. The built app bundle contains no crash-probe executable. Portable RED/GREEN/final reports, logs, toolchain and source/test summaries are under `t05/discard-process-crash/`; actual interrupted files and `.xcresult` remain under `Artifacts/t05-discard-kill-pr/`. T05 and the full product remain incomplete. No public shipment occurred.

## Increment 23 — durable scan-to-manual fallback

ManualDraft can now import a ScanDraft using a caller-supplied, explicitly confirmed CenterPalette and a strictly newer revision. It copies only manual sticker overrides in their already-corrected positions; it does not classify raw measurements, assume a manufacturer arrangement or prefill unknown cells. Supplied center assignments populate the six centers, including uncaptured faces. All other cells remain empty. The existing bounded manual archive and legality flow apply unchanged.

SessionController exposes switchScanToManual and a saving/failed status with explicit retry. It requires confirmation and scan context, checks revision overflow, and retains the old context until the concrete manual write and validated restoration succeed. Capture/playback stop immediately, solver work is cancelled, generations reject old frame/write delivery, and the ordered queue settles earlier writes before replacing the shared draft. Pending accepted corrections are retained even when their scan-save callback has not yet succeeded. Unaccepted freezing/review states require accepting or leaving the review before fallback, preventing silent loss of unaccepted edits. The eventual UI must obtain all six center assignments before invoking the transition; no camera access is required for a restored scan's fallback.

Failed fallback remains blocked for retry or confirmed global deletion; cancelling in memory cannot undo a possibly committed draft. Deletion supersedes pending fallback and prevents resurrection. The retained guide file stays unchanged and is not treated as the new manual draft's guide. Manual input must be validated normally, and a solved input originating from a verification scan can only produce enteredColorsSolved after this fallback—not camera verification.

Behavioral RED: model tests failed four assertions for lost rotated overrides and missing revision guards (`Artifacts/t05-manual-fallback-model-red.log`). After implementation the model passed. Two coordinator tests then failed eight assertions against callable no-op methods (`Artifacts/t05-manual-fallback-controller-red.log`) before orchestration was implemented. Eight new tests now cover mapping/unknown cells/bounded archive, confirmation/save barrier, retained guide bytes, post-write failure/retry, pending scan correction and stale camera callback, deletion during fallback, all four filesystem write boundaries, absent-scan/overflow guards and manual-only completion origin. The affected suite passed all 162 session tests. Installed-app fixture composition now exercises scan capture→manual fallback→restoration→discard→deletion; this is not a live-camera or completed UI claim.

This closes the model/coordinator fallback path, not T05 or the full manual-entry product. Final classified-scan review/acceptance, remaining lifecycle/mutation qualification, root screens, production camera/image processing, physical/media tests and distribution remain open. Dedicated process-kill tests for scan-to-manual conversion are not claimed; the four conversion boundaries use real files with injected errors, while the existing 33 process-kill cases cover the shared writer and other named operations. No public shipment occurred.

Increment 23 full regression: `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t05-manual-fallback-pr Scripts/test-pr.sh` exited 0. All 236 Swift package tests (162 session), 42 Python tests, 33 existing host process-kill cases and seven simulator app/UI tests passed. No simulator tests failed or skipped. Solver checks passed 10,000 PR states, 46,741 shallow states, named fixtures and pinned-reference comparisons. Non-document source hashes match the pre-test snapshot. Portable RED/GREEN logs, source/toolchain/test summaries and package output are under `t05/manual-fallback/`; full logs and `.xcresult` remain under `Artifacts/t05-manual-fallback-pr/`. T05, full product UI, production camera, media, physical qualification and distribution remain incomplete. Nothing publicly shipped.
