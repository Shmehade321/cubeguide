# T11 — Harden and qualify release build

## Current local automation

The current working source passes 285 Swift package tests and 21 infrastructure tests. Its latest app-only Xcode run passes 56 tests with zero failures or skips on the explicitly selected iPhone 16 Pro iOS 26.5 simulator. The preceding clean PR plan passed 66 tests, including 20 end-to-end UI tests. A generic unsigned Release device build succeeded, bundled `phrases.json` and E01–E03, compiled the app icon catalog, and completed Xcode's shallow store validation. Candidate status still depends on rerunning the complete plan against the exact clean commit.

The prior same-day full PR solver gate passed the 10,000-state seeded corpus, all 46,741 shallow states, eight named cases and pinned-reference comparisons, plus 33 process-kill storage cases. The app-only A01/A04/E01–E03 changes after that run do not alter CubeKit or solver resources, but this is still not a release-candidate million-state result.

## Qualification blockers

`Scripts/validate-assets.sh` now fails only for A08 and N01–N30. `Scripts/check-qualification.py release` stops because `docs/evidence/release.json` does not exist; creating passing rows without the required external evidence would be false. The one-million-state release corpus, mutation run on an exact candidate commit, 100 consecutive full physical cycles, low-storage and upgrade trials, live camera/image corpus, disconnected installed build, device latency/memory/thermal/frame measurements, iOS 18 minimum-OS renderer qualification and signed packaged-app audit remain unrun. T11 is not complete.

## Hardening continuation — 21 September 2026

The release script now directly runs table regeneration/independent validation plus core, solver and session mutation catalogs before asset and evidence gates. Qualification accepts only typed, checksummed JSON artifacts for the exact clean commit, validates report-specific evidence kinds, requires physical device identity for device reports, listening sign-off for media and an archive checksum for archive reports. Legacy path-only ledgers, stale commits, unknown kinds, invalid timestamps and dirty source are rejected.

On the current working source, all ten tables regenerated byte-identically and all independent table mutations were detected. All 12 core, 12 solver and 17 session mutations were killed with session sources restored byte-identically. Infrastructure tests pass. Generic unsigned Release analysis succeeds. These are local pre-candidate results; the million-state corpus and a fresh clean full Release plan still must run on the final commit, and external device/media/archive gates remain open.
