# T07 — production graphics and animation (running)

Base: 387d670, after calculation UI qualification 2b5ab90. Implementation sequence: ../t07-rendering-plan.md. Canonical scope remains Documentation/cube-app-implementation-plan.md T07, R07/R08/R12/R16 and V02/V10/V14. No graphics adapter, rendered asset or physical qualification is claimed by the mathematical work below.

## Increment 1 — exact scene geometry (in progress)

Added pure CubeCore types CubePosition, CubeStickerPlacement, CubeGeometry and CubeRotation. Canonical geometry has exactly 26 noncentral integer cubie coordinates and 54 ordered URFDLB sticker placements, including outward normal and upward edge. Public positions originate from the geometry; no unchecked external decoder or floating-point state is introduced. Pose application maps exact basis axes through CubeOrientation. Layer membership is the outward-axis dot product equal to one. CubeRotation maps typed turns/regrips to exact proper rotations and exposes the shortest signed animation quarter-turn angle. It never updates puzzle state or physical progress. RealityKit remains outside CubeCore.

Behavioral TDD evidence:

- Placements: minimal empty declarations compiled, then failed two assertions (26 cubies and 54 stickers), Artifacts/t07-geometry-placements-red.log. Implementation passed all 32 Core tests, t07-geometry-placements-green.log. Expected positions are handwritten triples derived from the documented outside-view/top-neighbor convention, not generated by the production placement function. Ownership checks require eight corners, twelve edges and six centers with distinct sticker normals.
- Poses: a no-op point transform failed at a missing geometric destination, t07-geometry-poses-red.log. Integer basis mapping then passed all 33 Core tests, t07-geometry-poses-green.log. All 24 poses match the independently qualified canonical view grids; top directions agree with the physical row above each center.
- Layers/rotations: no-op membership and rotation declarations failed four tests/four assertions, t07-geometry-rotations-red.log. Implementation passed all 37 Core tests, t07-geometry-rotations-green.log. Coverage includes nine-cubie/21-sticker membership per face/pose, all 18 turns × 24 poses × 54 sticker destinations against existing independent golden permutations, six regrips × 24 poses with exact inverses and preserved sticker identity, and animation-angle agreement with an independent integer Rodrigues calculation.

The existing 46,741-state shallow oracle and all other Core tests remained green. These results prove discrete geometry only; they do not establish floating-point RealityKit endpoints, long-run rendered snapping, lighting, visible arrows, labels, Reduce Motion, accessibility, performance or physical comprehension.

Full package regression is running as exec session 38001, Artifacts/t07-geometry-package-console.log and Artifacts/t07-geometry-package/. Resume it rather than restarting; do not modify Swift sources while live. The RealityKit adapter, actual editor preview, rendered endpoint/lifecycle tests, visual inspection, full PR regression and coherent increment commit remain pending. T06 retains its dependent 3D/guidance work as running.

Full package regression session 38001 completed exit 0: 265 tests (37 Core, 33 Solver, four Generator, ten Scan, 181 Session), all passed. The recorded geometry source hashes remain unchanged. Focused RED/GREEN and complete package logs are preserved under t07/geometry/. There are no live test/build sessions at this checkpoint. Next work is the actual RealityKit scene adapter and its rendered entity/material/endpoint tests, followed by editor integration, visual review and full PR qualification before committing this coherent rendering increment. No T07/full-product completion is claimed.

## Actual scene and editor integration

CubeSceneModel now builds 26 procedural beveled body entities with 54 sticker entities. Immutable ManualDraft cells supply material colors; missing cells use distinct gray question-mark markers. Seven bounded system-font glyph meshes are shared for semantic labels and unknown markers. Label preferences suppress known-color letters only; unknown markers remain. Pose updates reuse body/sticker identities and restore positions and face bases from exact geometry, without changing the draft.

Three initial app integration tests failed three behavioral assertions against the minimal empty model (Artifacts/t07-scene-red.xcresult, exit 65). The first implementation passed placement and label/update cases but failed seven color assertions because the test compared sRGB UIColor objects with the material reader's observed linear-RGB values. Corrected the test to convert both CGColors into explicit sRGB with numerical tolerance; production material colors were unchanged. All three tests passed (t07-scene-colors-green.xcresult). This was a test color-space mismatch, not a discovered wrong-color product defect.

The new PreviewUITests test failed at the missing editor.preview3D button (t07-preview-ui-red.xcresult, exit 65). The editor now pushes CubePreviewScreen, which keeps the practice banner outside the navigation stack, offers read-only view rotation/reset and named Front/Top/Right colors, and returns to the unchanged editor. CubeSceneView uses ARView.nonAR with automatic session configuration disabled, a virtual PerspectiveCamera and two procedural directional lights. It starts no capture or application-owned animation loop. Teardown detaches the cube and anchors and hides the view.

The initial integrated build failed for a missing CubeScan import; fixed it and did not count that compiler failure as behavioral RED. The next focused run passed four tests (three scene, one UI), zero failures/skips (t07-preview-integrated.xcresult). Visually inspected the exported light portrait screenshot: the actual 3D cube, beveled gaps, color letters, question mark, pose anchors and controls are readable; the practice banner remains visible.

Added direct virtual-camera/teardown coverage and extended the real manual persistence UI test to inspect its seven entered/47 unknown sticker preview, return, and require the original Blue sticker and missing-count. Ran four scene tests and both preview/manual UI paths with simulator appearance explicitly set to dark and parallel testing disabled. Xcode passed six tests with zero failures/skips (t07-preview-dark.xcresult); the wrapper restored the original light appearance in finally. Exported and visually inspected both dark screenshots: unknown markers are explicit and readable, and the practice example is distinct from real input. These are portrait iPhone 16 Pro/iOS 18.5 simulator checks, not all-size/Dynamic Type/VoiceOver or physical qualification.

The static preview is implemented but remains uncommitted pending full PR regression. Animated layer/regrip presentation, arrows, Reduce Motion action comparisons, guide integration, icon/help/completion art, broader layout/accessibility, rendered long-run snapping and physical/device/media/distribution gates remain open. No T07 completion or release is claimed.

## Full regression continuation

Resumed and confirmed live exec session 58773 for `SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t07-preview-pr Scripts/test-pr.sh`. Completed stages passed 265 package tests (37 Core, 33 Solver, four Generator, ten Scan, 181 Session), 45 Python tests, 33 host process-kill cases, 10,000 PR solver states, 46,741 shallow states, eight named cases and pinned-reference comparisons. Non-document source SHA-256 values match the pre-run snapshot. Portable completed-stage reports and logs are in t07/regression/.

The simulator UI stage remains running; this is not a full-run pass. Artifacts/t07-preview-pr/active-run.json records the polled live handle. Resume that handle without restarting or modifying Swift sources during the run. Final simulator counts, screenshots, source identity and commit remain pending. Static preview does not complete T07 animation/assets/accessibility or the full product/media/device/distribution goal.

## Increment 1 regression and scope

Full PR session 58773 completed with exit 0. The final result bundle reports 21 executed simulator tests (ten app integration, eleven UI), zero failures/skips/expected failures and no runtime warnings. All 265 package tests, 45 Python tests, 33 existing host process-kill cases, 10,000 PR solver states, 46,741 shallow states, eight named cases and pinned-reference comparisons passed. All non-document inputs match their pre-run SHA-256 values. Portable reports, compressed logs, source identity and current screenshots are retained in t07/regression/.

Exported and inspected the final light manual and practice screenshots: entered colors, distinct unknown question marks, face-color anchors and view controls are visible; the practice banner and exit remain clear. Earlier focused dark screenshots qualify that tested appearance as recorded above. These are portrait iPhone 16 Pro/iOS 18.5 simulator observations, not all-size, Dynamic Type, VoiceOver or physical-device qualification. Xcode logs include debugger-version lookup diagnostics; the completed result reports no runtime warnings or failed tests.

This coherent increment provides exact geometry and an actual read-only static preview for incomplete manual and isolated practice input. It does not animate moves or regrips, implement GuidePlayback, acknowledge physical moves, complete Reduce Motion guidance, supply all A-series art or close T07. T06 remains running for guidance and broader UI qualification. Camera, human recordings, physical/accessibility/novice qualification, signed archive and distribution remain open. Nothing has been uploaded or publicly released.

## Increment 2 — animation timeline (in progress)

Static preview was committed as 921226b. Added PreviewTimeline, a deterministic presentation-only value using Duration timestamps from one adapter-owned monotonic clock origin. It owns no GuideProgress, save or acknowledgement. The 0.5-second before hold precedes a 1.2-second quarter/regrip or 1.8-second half rotation; Slow doubles and Fast halves rotation duration. Pause samples current progress and excludes paused wall time; resume retains fractional progress; replay resets the hold. Duplicate Play and obsolete timestamps cannot reset/rewind an active preview. Stop restores idle/before state.

Three tests failed behaviorally against callable no-op declarations, 171 issues and exit 1 (Artifacts/t07-timeline-red.log). Implementation passed all three focused tests, exit 0 (t07-timeline-green.log). They exercise every specified speed, turn amount and regrip, hold/end boundaries, fractional progress, pause/resume/replay, completed-state clamping, stale time samples and stop/restart. Full package regression completed exit 0 (t07-timeline-package.log). Portable transcripts and source hashes are under t07/timeline/.

This timeline remains uncommitted within the animation increment. It is not yet consumed by RealityKit or GuidePlayback and does not establish actual motion, completion-callback identity, narration sequencing, Reduce Motion or physical progress. Next: failing actual-scene layer/regrip tests, exact snapping and lifecycle integration; then focused rendered inspection and full PR regression before commit. T07 and all remaining product/media/device/distribution gates remain open.

### Actual scene sampling and cancellation

CubeSceneModel now prepares an immutable GuideAction before-state and groups nine turn-layer bodies or all twenty-six regrip bodies under an identity pivot. Absolute fractional samples rotate that pivot using the specified signed axis/angle; repeated samples do not accumulate transforms. Completion reparents all bodies, removes the pivot and rebuilds exact resting positions, sticker bases and colors from action.after/toPose. Cancellation reconstructs action.before/fromPose. No scene operation edits Session or acknowledges a physical move.

Two new app tests failed behaviorally against no-op preview methods (288 issues; Xcode exit 65, t07-animation-red.xcresult). Implementation passed those plus four existing scene tests (six total, exit 0, t07-animation-green.xcresult). Tests inspect selected parent groups, independently calculated halfway angles for three front-turn amounts, stationary bodies, repeated samples, cancellation and every planner move/pose endpoint. This is entity-transform evidence, not inspected live animation or full arrow/regrip comprehension.

A further test exposed cancellation after an already-finished preview retaining after-state colors (t07-animation-cancel-red.xcresult, exit 65). The scene now retains its immutable preview context after snapping, ignores further samples once finished, and restores the before-state on cancellation before clearing that context. Seven scene tests passed in t07-animation-cancel-green.xcresult, exit 0. Portable logs/summaries and exact source hashes are under t07/animation/.

Timeline and scene sampling remain uncommitted. Remaining increment work includes live GuidePlayback/timeline ownership and callback identity, all six regrip interpolation cases, long sequential snapping, interruptions/teardown, actual rendered animation inspection, overlays and Reduce Motion integration, and full PR regression. No complete T07, media/device or release qualification is claimed. No build/test process remains live at this checkpoint.

### Playback/timeline/scene adapter

GuideScenePlayback now implements GuidePlayback around the real PreviewTimeline and CubeSceneModel. A PreviewFrameSource boundary supplies update callbacks; an injected monotonic timestamp provider supports deterministic boundary tests. Each play subscription has a fresh UUID. Pause stops frame delivery and clears the old callback while retaining sampled progress; resume accepts a new PlaybackID without counting paused time. Replay rebuilds the before-state and timeline using current speed preferences. Stop invalidates delivery and restores the before-state. Completion stops frames and clears its callback before reporting the current PlaybackID once; it never sends a physical acknowledgement.

Two new tests failed behaviorally against no-op adapter methods (two assertions; t07-playback-red.xcresult, exit 65). Implementation passed both plus the seven existing scene tests (nine total, zero failures/skips; t07-playback-green.xcresult, exit 0). Tests inspect real halfway entity transforms and retained pause progress, explicit old callback delivery after resume/replay/stop, Fast half-turn completion boundaries, reset and exactly-once completion. The frame source is a controlled adapter fixture; this is not production display refresh or installed guide UI evidence. Portable summaries/logs/source hashes are in t07/playback/.

Production frame-source lifecycle, screen ownership/controller integration, all regrip interpolation and long-sequence checks, arrows/Reduce Motion and real rendered animation inspection remain pending. This animation increment remains uncommitted and requires full PR regression before commit. No live build/test process remains at this checkpoint. No media/device/distribution gate is closed.

### Production display ticks and monotonic clock

DisplayLinkFrames implements PreviewFrameSource with CADisplayLink on the main run loop in common modes. Stop invalidates the link and clears its callback; replacement stops the old subscription first. A weak target prevents the run loop from retaining the source owner; an orphaned link invalidates itself on its next delivery. Explicit pause/exit/background stop remains required at screen integration.

Two tests first failed because no callback arrived from the empty implementation (t07-displaylink-red.xcresult, two tests/two failures, exit 65). The real driver passed those plus the existing nine scene/playback tests (eleven total, t07-displaylink-green.xcresult, exit 0). The tests observe actual run-loop delivery, absence after stop/replacement, and owner release. Bounded XCTest waits establish callback lifecycle, not frame-rate or physical performance qualification; the replacement delivery timeout was broadened from 0.2 to 2 seconds before GREEN to avoid treating scheduler latency as a performance requirement.

The production GuideScenePlayback initializer now creates this driver and samples elapsed Duration from ContinuousClock. An integration test initially failed to compile for a missing RealityKit import; corrected before behavioral RED. With a fixed zero clock, the test then failed two assertions (missing completion and unsnapped pivot), t07-live-playback-behavioral-red.xcresult, exit 65. Wiring the real monotonic clock passed all twelve scene/playback/display-source tests, zero failures/skips (t07-live-playback-green.xcresult, exit 0). This exercises real display callbacks through the real timeline and actual scene; it is not a rendered user-screen or physical frame-rate check. Portable logs, summaries and source hashes are under t07/displaylink/.

The animation increment remains uncommitted pending screen/controller ownership and lifecycle integration, remaining regrip/sequence/overlay/Reduce Motion checks, visible animation inspection and full PR regression. No test/build process remains active at this checkpoint. Media, physical qualification and distribution are still open.

### Regrip interpolation and sequential snapping coverage

Added coverage for all six regrips, including roll operations not generally needed by shortest front-face plans, at every one of the 24 poses. Tests use explicit viewer-axis/sign fixtures for halfway quaternions and inspect every body position and sticker normal. Test-only internal GuideAction construction supplies those otherwise uncommon operations; no production constructor visibility changed.

A second test drives the same actual scene through 1,000 sequential face turns and their planned regrips, periodically cancelling and replaying partial previews. Every completed action is checked against exact canonical positions, sticker colors and pose; body identities and the bounded sticker count survive the sequence. These tests passed existing behavior and are recorded as additional coverage, not fabricated new-feature RED cycles.

The focused run passed fourteen tests, zero failures/skips (t07-animation-sequences.xcresult, exit 0), covering scene, playback and real display clock together. Portable results/logs/source identity are under t07/animation/. This is simulated entity-transform and lifecycle evidence, not physical sustained frame rate or user comprehension. No build/test process remains active.

Composition inspection confirms SessionController owns an immutable injected playback dependency. The upcoming guide integration must therefore create a persistent presentation owner at real/practice composition time and preserve it across SwiftUI state changes; replacing the controller or creating per-render players would lose the existing durability/lifecycle guarantees. Screen/controller integration, arrows, Reduce Motion, visual animation review and full regression remain pending before this increment can be committed.

### Persistent guide presentation ownership

GuidePresentation implements the injected GuidePlayback boundary and weakly binds to one SessionController. It owns the actual scene and player across repeated screen preparation. A matching action/palette leaves active transforms unchanged; a changed pending action stops prior playback and reconstructs its before-state while retaining the scene when the palette is unchanged. Replacement controllers clear old presentation state. Play requires the controller's exact pending action and matching PlaybackID action; invalid delivery stops and requests the existing background/comparison path instead of manufacturing completion.

A real-controller integration test starts isolated practice with the real solver and memory store, obtains a durably prepared guide, and initially failed at the no-op prepare boundary (t07-presentation-red.xcresult, one test/one failure, exit 65). After implementation it checks stable scene identity/transforms across preparation during playback, preview completion with zero acknowledged actions, explicit acknowledgement followed by actual save/next preparation, and background comparison with stale frames ignored. The controlled frame source supplies deterministic callbacks; actual display-clock tests run alongside it.

All fifteen focused scene/playback/display/presentation tests passed, zero failures/skips (t07-presentation-green.xcresult, exit 0). Portable logs/summaries/source hashes are in t07/presentation/. This owner is not yet wired into the production ContentView or PracticeView composition, and no visible guide screen is claimed. Next: that wiring and guide UI behavioral RED/GREEN, overlays/Reduce Motion, rendered inspection and full regression. The animation increment remains uncommitted; no process remains live at this checkpoint and no release occurred.

### Initial installed guide UI

The pending GuideUITests run completed with the intended behavioral failure at missing guide.align (one executed test, one failure, exit 65; t07-guide-ui-red.xcresult). ContentView and PracticeView now inject and bind a persistent GuidePresentation before controller use. Its production initializer uses the actual display driver and monotonic clock. GuideFlowView keeps that scene across preparation, guide, acknowledgement saving and resume phases; SwiftUI updates change only the view background rather than resetting the animated model.

The focused GREEN run completed exit 0: one UI test, no failures/skips/expected failures or runtime warnings (t07-guide-ui-green.xcresult). It exercises real practice validation/solver, required alignment, preview/replay without physical advancement, disabled acknowledgement during playback, explicit acknowledgement advancing an action, and Home/resume/exit. Exported and inspected the light portrait screenshot: actual lettered cube, exact action caption, expected-after label, pose anchors, controls and persistent practice banner are readable. Portable logs/results/screenshot/source hashes are in t07/guide-ui/.

This is an initial incomplete guide screen, not completed T07/T08. Before/after comparison choices, uncertain recovery and completion UI, overlays, Reduce Motion and accessibility normalization, narration/effects/haptics, broader visual qualification and full PR regression remain open. No increment commit or release is claimed. The next expanded UI RED is running as exec session 68644, Artifacts/t07-guide-comparison-red.log/.xcresult; it requires showing the saved expected-after view without changing progress and then explicitly acknowledging that match. Resume the live handle; do not edit Swift while it runs.

Comparison continuation: session 68644 completed exit 65 at missing guide.showAfter, the intended behavioral RED. Added explicit before/expected-after scene selection restricted to resume/storage comparison; inspecting either view does not send an acknowledgement. Matches after uses the reducer's existing durable acknowledgement barrier. The first GREEN attempt failed compilation because the UI referenced private failedSaveKind; removed that reference and retained the public durable-preparation guard. That build failure is not counted as behavioral evidence. The corrected run t07-guide-comparison-green2.xcresult completed exit 0 with one UI test, showing unchanged progress after expected-after inspection and a changed action count only after explicit Matches after. Portable RED/build-failure/GREEN logs and result summaries are in t07/guide-ui/.

Next: uncertain/mismatch recovery and completion controls, full before/after rendering assertions, partial playback labeling, pause/background/teardown and auxiliary-navigation cases, overlays/Reduce Motion/accessibility, visible motion review and full PR regression. Current code is incomplete and uncommitted. No task/release completion is claimed.

Affected installed-app integration suite completed exit 0 (t07-guide-integration.xcresult): 21 passed, 0 failed, 0 skipped. It includes the existing actual scene, timeline/playback/display-driver, persistent presentation and app composition tests. This is affected regression only, not full PR qualification or completed guidance. Portable summary/log and current comparison source hashes are under t07/guide-ui/. No process remains live at this checkpoint.

### Recovery/completion UI RED qualification

Added real-practice UI tests for mismatch/uncertain recovery, keeping the existing guide, cancelling and confirming manual replacement, and separate physical completion confirmation after every planned action is played and explicitly acknowledged. The first incremental run (t07-guide-recovery-red.xcresult, exit 0) discovered only the old test despite compilation of the changed test file; it is rejected as evidence for the two added tests. Corrected the test's center-assignment title against the existing screen before a fresh build.

Fresh run t07-guide-recovery-fresh-red.xcresult discovered all three tests: the original guide/comparison path passed, the completion test failed after the last action because the final confirmation screen is absent, and recovery failed at missing guide.mismatch. The xcodebuild process remains live as exec session 80811 collecting simctl diagnostics (child 68166 observed), so no production source edits have begun. Continue polling that exact handle; do not restart on an observation timeout. These are the intended behavioral failures; no GREEN or completed UI is claimed.

Recovery/completion RED session 80811 exited 65 after the simulator-diagnostic timeout. The completed result reports three tests, one passed and two expected behavioral failures; no test was skipped. Completion trace contains ten actual Play and ten acknowledgement taps before the missing final screen. The original incremental run remains rejected for discovering only one test. Portable final logs/summaries are retained; the active-run record is now terminal.

Implemented mismatch/uncertain controls through the existing durable recovery reducer, recovery keep/compare and explicit confirmation before manual replacement, and expected-solved confirmation with clear non-camera wording. Saving/failure states reuse the existing save/retry path. The actual camera-rescan/verification controls remain dependent on T09 and are not fabricated. GREEN is running as session 86149, Artifacts/t07-guide-recovery-green.log/.xcresult; no success, task completion or commit is claimed yet.

Recovery/completion first GREEN attempt exited 65: completion and comparison passed, while recovery failed waiting for a hittable uncertain button. Exported accessibility evidence places it at y937.7 in an 874-point window inside the ScrollView. The test now performs a user scroll before choosing it; production behavior was unchanged for this correction. The subsequent t07-guide-recovery-scroll run passed all three UI tests with zero failures/skips/expected failures/runtime warnings. Inspected retained completion and recovery screenshots: readable instructions and controls, persistent practice label, distinct user confirmation, and explicit manual replacement. This is one portrait simulator size, not full visual/accessibility qualification.

Source review found Home rejected from expectedSolved. A new pure regression failed three assertions (t07-completion-home-red.log). Before implementation, the authoritative transition table was clarified with a dated execution refinement: exit preserves unconfirmed final progress; Resume still requests confirmation. The reducer now accepts that exit, and its phase/event matrix reflects the clarified contract. All 269 package tests passed (37 Core, 33 Solver, four Generator, ten Scan, 185 Session), t07-completion-home-package-green.log. The UI completion test additionally checks Home/Resume before physical confirmation.

Combined actual-app integration plus three guide UI tests are running as session 7234, Artifacts/t07-guide-recovery-integrated.log/.xcresult. Resume it; no Swift edits while live. Full PR, remaining visible motion/lifecycle/overlays/Reduce Motion/media/accessibility qualification and coherent commit remain pending. Camera recovery and scan verification still require T09; no camera or release claim is made.

Combined recovery integration session 7234 completed exit 0: 24 tests (21 app integration plus three guide UI), zero failures/skips/expected failures/runtime warnings. The completion trace includes the added Home/Resume interaction before physical confirmation. No completed status is granted by leaving that screen.

A presentation review identified that moving/paused previews still displayed the before-state label. Extended the actual controller/scene test through halfway playback, pause, resume and completion; a callable constant label failed three assertions (t07-guide-label-red.xcresult, 21 tests/one failed/three issues, exit 65). GuidePresentation now derives its description from actual controller preview state, and the screen consumes it while retaining explicit comparison selection labels. The corrected app integration run passed all 21 tests, zero failures/skips/runtime warnings (t07-guide-label-green.xcresult, exit 0). Portable logs/results and source hashes are in t07/guide-ui/.

No process is live at this checkpoint. This work remains uncommitted until its full PR regression. T07/T08 remain incomplete: overlays, Reduce Motion and accessibility normalization, broader motion/layout inspection, final artwork, audio/haptics, camera recovery/verification and physical qualification remain required. No archive or distribution work was performed.

Full PR regression has started as exec session 13079: SIMULATOR_UDID=67DB7428-A25B-4167-8FC2-24F47A392E81 ARTIFACT_DIR=Artifacts/t07-guide-pr Scripts/test-pr.sh, console Artifacts/t07-guide-pr-console.log. Resume the exact handle rather than restarting; no Swift-source edits while it runs. Expected discovery includes all three GuideUITests plus the 21 app integration cases; verify actual counts and the new completion Home/Resume trace. The active-run record is under Artifacts/t07-guide-pr/. No full-run pass, commit or release is claimed yet.

Full regression continuation: session 13079 is confirmed live in solver qualification. Completed stages passed all 269 package tests, 45 Python tests and 33 actual host process-kill cases. Non-document source inputs still match the pre-run snapshot. Portable completed-stage logs, storage report, toolchain and source identity are under t07/guide-regression/. The PR solver corpus and clean simulator stage remain in progress/pending; no full regression pass or commit is claimed. docs/t07-rendering-plan.md now records the next accessibility/overlay increment's concrete behavioral boundaries while the tested Swift sources remain unchanged.

### Full-regression failure and presentation test scheduling

The initial full PR session 13079 ended with exit 73 after deliberate interruption of the remaining UI stage. All 21 app integration cases executed, but production realPlayback failed its five-second completion wait while exhaustive MainActor scene tests ran concurrently. The scene endpoint assertion passed. This run is failed/incomplete, not a passing regression; its original logs and result bundle are retained.

Grouped the five scene/playback/presentation test suites beneath one Swift Testing @Suite(.serialized) parent, leaving production behavior, test cases, assertions and timeouts unchanged. This prevents exhaustive synchronous scene work from overlapping live display-clock deadlines within that family. Fresh DerivedData/default-parallel Xcode run t07-serialized-green completed exit 0: all 21 app integration tests passed, zero skipped/expected failures and no xcresult runtime warnings. Real playback completed in 1.113 seconds. Console RealityKit material/shader diagnostics remain present; zero structured warnings does not mean a silent console or physical graphics qualification. Portable summary and original logs are in t07/guide-regression/. Full PR must now run against this test structure before commit.

Fresh full PR regression is active as session 76719, Artifacts/t07-guide-final-pr, console Artifacts/t07-guide-final-pr-console.log. Resume that handle; Swift sources remain fixed during qualification. No full pass or commit yet.

Fresh regression checkpoint: session 76719 is confirmed live in solver qualification. Completed stages passed all 269 package tests (37 Core, 33 Solver, four Generator, ten Scan, 185 Session), 45 Python tests and 33 actual host process-kill cases. Non-document inputs match the pre-run SHA-256 snapshot. Portable completed-stage evidence is under t07/guide-regression/final/. Solver and clean simulator completion remain pending; this is not a full-pass or commit claim.

Fresh regression continuation: all 10,000 PR states, 46,741 shallow states, eight named cases and both pinned-reference comparisons passed; zero solver timeouts. All 21 app integration tests also passed under the full runner default settings, including real display playback in 1.107 seconds. Simulator UI tests remain live in session 76719; the final result bundle/counts, screenshots and full-run source identity are still required before commit. Solver summaries are copied under t07/guide-regression/final/solver/.

## Increment 2 final regression and scope

Fresh full PR session 76719 completed exit 0. The final result reports 35 executed simulator tests (21 app integration, fourteen UI), zero failures/skips/expected failures and no structured runtime warnings. All 269 package tests, 45 Python tests, 33 host process-kill cases, 10,000 PR states, 46,741 shallow states, eight named cases and pinned-reference comparisons passed. Non-document source hashes match the pre-run snapshot. Original failed concurrent-clock evidence remains retained; no timeout/assertion was relaxed for the scheduling fix.

Exported and visually inspected the three final guide screenshots: the expected-after cube, letters, anchors and controls are visible; recovery offers explicit keep/replace; completion requests physical confirmation and explicitly disclaims camera verification. Practice labeling and exit remain visible throughout. The complete UI trace includes Home/Resume before physical confirmation. These are light portrait iPhone 16 Pro/iOS 18.5 simulator observations, not full layout/VoiceOver/device qualification or inspected mid-turn direction. Portable final summaries, logs, source verification and screenshots are under t07/guide-regression/final/.

This coherent increment delivers the deterministic timeline, actual pivot animation/snapping, production display clock, persistent guide ownership, alignment, preview/replay/pause, explicit durable acknowledgement, before/after comparison, uncertain/manual recovery and separate physical completion confirmation. It does not complete T07/T08. Arrows/highlights, Reduce Motion, live accessibility/label preference normalization, remaining artwork, coordinated human narration/effects/haptics, camera recovery/verification, broader visual/lifecycle/device qualification and release distribution remain required. No archive, TestFlight, submission or public release occurred.

## Increment 3 — live label policy (in progress)

The animation/initial guide increment was committed as b04eca9 after its full PR pass. A new actual-controller/scene test exercises Differentiate Without Color with the saved label preference off, toggles it halfway through playback, checks completion/replay/comparison, then changes the saved preference. It requires 54 glyphs when enabled, unchanged body transforms, unchanged saved preference under system override and zero acknowledged actions.

The first attempt failed compilation because the test needed @testable import CubeSession for its existing effect-wait helper; this is not behavioral RED. The corrected run executed 22 app integration tests and failed five label assertions (t07-label-policy-behavioral-red, exit 65). Glyph-only refresh now preserves active pivots/transforms and updates the retained preview policy for snapping/cancellation. GuidePresentation supplies normalized preferences to preparation, playback and comparison; GuideFlowView observes initial/system/preference changes. No saved preference is overwritten by the accessibility override.

Affected GREEN passed all 22 app integration tests, zero failures/skips/expected failures/runtime warnings (t07-label-policy-green, exit 0). Portable original logs, summaries and source hashes are under t07/labels/. This is controller/scene evidence; rendered SwiftUI environment/preference propagation, broader visual qualification and full PR regression remain required. The label changes are uncommitted within the next presentation increment. Reduce Motion, direction overlays and remaining artwork/media/device/distribution work remain open. No process is live at this checkpoint; no release occurred.

### Hosted label propagation and rendered input

Added an actual UIHostingController/GuideFlowView test using the real practice controller, saved preferences and controlled preview frames. SwiftUI's system Differentiate Without Color environment is read-only; an attempted direct injection did not compile and is not counted as behavioral evidence. The final hosted test checks saved-preference propagation while the existing direct presentation test checks the system override policy. Physical system-setting qualification remains open.

The first hosted runs reported transform differences and a missed label refresh. Waiting until the actual scene is attached removed initial-mount ambiguity without relaxing exact transform assertions. A diagnostic mounted run passed, but a later run with production sources restored still failed its final post-pause label-refresh assertion. This is retained as a failure, not retried away. The test also requires accepted preference saves and settled effects.

GuideSceneSurface now receives effective label visibility as a declarative input and applies glyph-only updates in updateUIView. This replaces the preference-only onChange side effect; preparation/playback still normalize the system override. The unchanged hosted test plus all other app tests passed: 23 executed, zero failures (t07-label-rendered-green). Logs and summaries, including all intermediate failures, are under t07/labels/. This is affected evidence, not full PR qualification.

A targeted mutation omits only the rendered glyph update. All 23 tests executed and the hosted test failed at missing labels, as intended. Xcode is still collecting simctl diagnostics in exec session 55496 (t07-label-render-mutant.log/.xcresult). The Python wrapper temporarily owns GuideFlowView.swift and restores its original bytes in finally after xcodebuild exits. DO NOT edit Swift, commit, or start another build while that session is active. Resume the exact handle and verify Artifacts/t07-label-render-restoration.json before the required post-restoration GREEN run. No mutation pass, source restoration, final regression or commit is claimed yet.

Rendered-update mutation session 55496 completed after simctl's diagnostic timeout, with Xcode exit 65. Its completed summary records 23 executed tests and exactly one hosted-label failure; the failed test is retained as mutation sensitivity evidence. The wrapper restored GuideFlowView.swift byte-for-byte, verified against its saved SHA-256. Post-restoration t07-label-restored-green completed exit 0: all 23 integration tests passed, zero skipped/expected failures/runtime warnings. Original logs, summaries, restoration proof and current source hashes are in t07/labels/. No live test/build/mutation process remains. Full PR and broader visual/accessibility qualification are still pending; label changes remain uncommitted.

Installed-app label visual coverage: added GuideUITests.testGuideRendersSavedLabelChoice, exercising actual Settings saves, separate practice creation, real solve/preview and unchanged action progress for both label choices. Fresh DerivedData run t07-label-ui executed exactly one selected test and passed with zero failures/skips/runtime warnings. Exported and inspected both light portrait screenshots: glyphs are visible with the preference on and absent with it off, while caption/pose anchors/controls remain visible. This is additional coverage of the implemented behavior, not another claimed feature RED. Physical system override and broader layout/appearance checks remain open. Portable logs, summary and images are in t07/labels/.

The label-policy and rendered-update changes will be committed as a coherent accessibility increment after full regression, before implementing the planned Reduce Motion/direction-diagram increment. This keeps the full T07 scope and acceptance gates unchanged.

Full label regression is active as session 53501, Artifacts/t07-label-pr, console Artifacts/t07-label-pr-console.log. Resume the same handle without restarting or changing Swift sources. Expected simulator discovery is 38 cases (23 app integration and fifteen UI); verify actual results rather than trusting the success banner. No full pass or commit is claimed yet.

Label regression checkpoint: session 53501 is confirmed live in solver qualification. Completed stages passed 269 package tests, 45 Python tests and 33 actual host process-kill cases. Non-document source hashes match the captured run inputs. Portable completed-stage reports/logs are under t07/labels/regression/. Solver and clean simulator completion remain pending; no full pass or commit is claimed.

Label regression continuation: the 10,000-state PR corpus, all 46,741 shallow states, eight named cases and both pinned-reference comparisons passed with no failures. Reports are under t07/labels/regression/solver/. Non-document source hashes remain unchanged. Session 53501 continues into clean simulator qualification; final counts, UI traces, screenshots and source identity are pending before commit.

The clean full-run app integration stage passed all 23 tests, including hosted preference changes and exact transform preservation. Calculation cancellation/failure UI, editor persistence/relaunch and the complete scramble-entry/consent/real-solver UI path have also passed. The solved-input UI path remains live in session 53501; final full-run qualification is not yet claimed. Non-document source hashes still match the run snapshot.

## Increment 3 final regression and scope

Full label regression session 53501 completed exit 0. The final result reports 38 executed simulator tests (23 app integration and fifteen UI), zero failures/skips/expected failures and no structured runtime warnings. All 269 package tests, 45 Python tests, 33 host process-kill cases, 10,000 PR states, 46,741 shallow states, eight named cases and pinned-reference comparisons passed. All 203 non-document source inputs match their pre-run SHA-256 values. Portable final summaries, compressed logs, source verification and screenshots are under t07/labels/regression/. Original behavioral failures, hosted propagation failures and the restored rendered-update mutation remain retained.

Exported and inspected both final label screenshots: the same expected-after cube and pose show letters with the saved preference on and no letters with it off. Caption, front/top/right anchors, controls and the practice banner remain readable in the tested light portrait simulator. The hosted test additionally verifies saved preference changes while playing and paused without transform changes; the direct controller test verifies the Differentiate Without Color override without overwriting preferences or advancing progress. This does not establish physical system-setting behavior, dark/large-text layouts or VoiceOver usability.

This coherent increment qualifies live label policy and rendered preference propagation. T07/T08 remain incomplete: Reduce Motion, direction overlays, remaining artwork, coordinated human narration/effects/haptics, camera recovery/verification, broader visual/lifecycle/device qualification and distribution remain required. Next is the documented static Reduce Motion and cancellable one-shot presentation boundary. No archive, TestFlight, submission or public release occurred.

## Increment 4 — static Reduce Motion timing (in progress)

Live labels were committed as 79423a2. The next increment starts with a remaining-duration contract on the existing monotonic PreviewTimeline. A new behavioral test failed 32 assertions against a zero-duration callable boundary (t07-reduced-deadline-red, exit 1). The implementation derives remaining time from the existing hold, rotation duration and sampled elapsed time. The complete package run then passed, including the new test and existing scene geometry, solver, scan and session suites (t07-reduced-deadline-package-green, exit 0). Literal cases check paused hold time, resumption, the millisecond before completion, endpoint, replay, stale samples and stop. Portable logs/source hashes are under t07/reduced-motion/.

This is timing groundwork, not a rendered Reduce Motion feature. Added two actual StaticGuidePlayback boundary tests with controlled one-shot deadlines retained after cancellation. They require fixed before-state until completion, paused-time exclusion, replay/replacement/stop invalidation and exactly-once callback identity. The callable no-op adapter is currently undergoing its RED run as session 48582, Artifacts/t07-static-playback-red.log/.xcresult. Do not edit Swift while it runs. The production one-shot scheduler, static view/direction diagrams, system-policy integration, visual review and full PR regression remain required before committing the coherent increment.

Static playback RED completed exit 65: 25 app integration tests executed, the 23 existing tests passed and both new static-playback tests failed at missing scheduled deadlines. No compiler failure or skip is counted. The adapter now uses the real PreviewTimeline with a one-shot scheduling interface, fixes the before/after selection until the matching deadline, and invalidates callback generations on pause/replay/replacement/stop. Early delivery reschedules only the remaining duration. Affected GREEN is active as session 36637, Artifacts/t07-static-playback-green.log/.xcresult; no pass or commit is claimed yet. This adapter is not wired into the user interface and has no production scheduler yet.

Static playback affected GREEN session 36637 completed exit 0: all 25 app integration tests passed, zero failures/skips/expected failures and no structured runtime warnings. The tests exercise the real static player and timeline with deliberately retained cancelled callbacks, including early delivery, pause/resume, replay, replacement, stop and exactly-once endpoint completion. Portable RED/GREEN summaries/logs and source hashes are under t07/reduced-motion/. Existing console RealityKit shader diagnostics remain separate from the structured warning count.

No test/build process remains live at this checkpoint. The Reduce Motion increment remains uncommitted and incomplete. Next: implement and qualify the real cancellable one-shot scheduler; integrate static before/after rendering and direction diagrams with system policy and physical-comparison transitions; inspect actual views and run full PR regression before commit. No release gate or T07/T08 completion is claimed.

Production scheduler continuation: two real scheduling tests failed at missing delivery against the callable no-op (27 executed, two failures; t07-deadline-driver-red, exit 65). PreviewDeadline now owns one cancellable ContinuousClock sleep task, invalidates replacements, and cancels on owner release. Its callback does not retain the owner across the sleep. All 27 app integration tests passed (t07-deadline-driver-green, exit 0), including delayed single delivery and cancellation/replacement/release. These bounded run-loop waits qualify scheduler lifecycle, not physical timing performance. Portable original logs/summaries are under t07/reduced-motion/.

Two new real-controller motion-policy tests are in RED qualification, session 85548, Artifacts/t07-motion-policy-red.log/.xcresult. They require static preparation with no RealityKit scene or frame subscription and physical comparison after a policy change during playback/pause. Minimal declarations currently compile; no UI/system-policy integration or complete Reduce Motion claim is made. Do not edit Swift while this run is active.

Motion-policy RED continuation: all 29 integration tests executed; 27 passed and the two new tests failed at (1) static preparation still creating a scene and (2) policy change failing to enter unaligned resumeCheck. The xcodebuild process remains live in session 85548; process inspection confirmed its simctl diagnose child with a 600-second diagnostic timeout. This is not a terminal or passing run. Artifacts/t07-motion-policy-red-active.json records the live handle. Resume that exact process; do not restart or change Swift sources until it exits.

After terminal RED evidence, wire the injected scheduler into GuidePresentation. Reduced mode must create/retain StaticGuidePlayback across preparation, avoid creating a RealityKit scene, and route play/pause/stop to that player. A changed system policy during playing/paused preview must send the existing background/comparison transition, invalidate both player callbacks and clear the old rendered ownership before preparing the selected representation. The static UI and direction diagrams remain subsequent required work, with separate rendered evidence and full regression before this increment is committed.

Motion-policy RED finalized exit 65 after stopping only the simctl diagnostic collector with SIGTERM; all tests had already completed. The final bundle retains 29 executed tests, 27 passes and exactly the two intended assertion failures, with no skips. Diagnostic collection is partial and is not presented as a test timeout or passing run. GuidePresentation now retains the injected one-shot scheduler, creates StaticGuidePlayback in reduced mode without a RealityKit scene, routes playback/pause/stop, and requires physical comparison when motion policy changes during playing/paused preview. Affected GREEN is running as session 38934, Artifacts/t07-motion-policy-green.log/.xcresult. No source edits while live; UI/static diagram integration and full regression remain outstanding.

Motion-policy affected GREEN session 38934 completed exit 0: all 29 app integration tests passed, no failures/skips/expected failures or structured runtime warnings. Reduced preparation creates no scene, playback requests no frame callbacks, and the actual controller preserves zero physical acknowledgements across static preview completion and policy interruption. This is injected-policy integration, not yet actual SwiftUI/system-setting evidence.

Static drawing RED is now running as session 41873, Artifacts/t07-static-drawing-red.log/.xcresult. Tests require 27 visible stickers with literal R-fixture colors in identity/yaw poses, row/column geometry, and viewer-space arrow endpoints for all three front turn amounts and six whole-cube regrips. The drawing consumes CubeCore pose/geometry, and direction strokes will consume CubeRotation. Minimal callable drawing constructors currently return no geometry. Keep Swift sources fixed until the RED process is terminal.

Static drawing qualification: the initial build failed because the test lacked Foundation/CoreFoundation member visibility; adding its explicit import fixed compilation and is not behavioral RED. The corrected run completed 31 tests with two intended failures at missing geometry. Its diagnostic child had already exited when targeted cleanup was attempted; Xcode finalized exit 65 normally. StaticCubeDrawing now projects CubeCore viewer placements and colors, and GuideDirectionStroke samples CubeRotation's signed axis for each operation. The affected GREEN passed all 31 integration tests, zero failures/skips/expected failures/runtime warnings. Literal R-fixture colors and all nine arrow endpoint directions passed. Geometry tests alone do not qualify a visible diagram.

Added an installed-app ReduceMotionUITests path that changes the actual simulator Settings accessibility toggle, opens real practice/solver/guidance, and requires static before/after views plus a direction diagram with unchanged physical progress. The test restores the motion setting afterward. Its first run is active as session 63419, Artifacts/t07-reduced-ui-red.log/.xcresult. It must fail at missing product UI before that UI is implemented; navigation/setup failures are not counted as that evidence.

Installed Reduce Motion UI RED completed exit 65 at missing guide.staticCube after successfully enabling the actual simulator accessibility setting and reaching real practice guidance. Teardown restored Reduce Motion off. StaticGuideView now draws discrete Canvas cube/arrow layers with the shared geometry, stronger moving-layer outlines, visible state/scope text and semantic accessibility elements. GuideFlowView observes the actual system policy and displays that static surface. The focused GREEN executed one test and passed (t07-reduced-ui-green, exit 0), covering actual Settings, before/after, replay, pause and unchanged progress; teardown now restores the original setting rather than assuming its prior value.

Inspected both initial light portrait screenshots: static before/after sticker layouts, front-layer arrow, layer outlines, caption, pose anchors and controls are visible. A duplicate state caption was removed after inspection. Added artifact generation for all nine operation directions in light/dark, each with before and expected-after views; this is additional visual coverage, not new-feature RED or automatic visual approval. The combined app-integration/rendered-catalog/system-UI run is active as session 87205, Artifacts/t07-reduced-visual.log/.xcresult. Source hashes are in Artifacts/t07-reduced-visual-source.json. Keep Swift fixed until terminal results, then inspect every catalog image. No full PR pass, completed Reduce Motion increment or commit is claimed.

The combined rendered run completed exit 0 with 33 tests (32 app integration/catalog and one system-policy UI), zero failures/skips. All 213 captured source hashes matched afterward. Visual inspection rejected the arrow-on-stickers layout: several arrows obscure color letters, and projected yaw cues require clearer context. Some dark catalog images also lose default text contrast; this needs correction and reinspection, not a visual pass. The new installed UI non-overlap assertion reproduced the layout failure after reaching the real static guide. Its run remains live as session 68475 until Xcode finalizes; no implementation edit while live.

Overlap regression RED finalized normally with one intended UI assertion failure (exit 65). The revised view puts its direction cue in a separate neutral schematic below the comparison cube. The combined GREEN completed 33 tests, all passed, and all 213 source hashes matched. Visible roll text now has contrast in the dark catalog; the installed light UI has unobstructed sticker letters and accessible alignment/play controls. Inspection found the half-turn schematic arrowhead slightly clipped at its lower edge, so an eight-point diagram inset was added. A fresh catalog plus actual dark-system UI run is active as session 35396, Artifacts/t07-static-diagram-dark.log/.xcresult. This is still visual qualification, not full PR qualification or increment completion.

The inset catalog plus dark-system UI run completed exit 0: two tests passed, no skips/failures, 213 source hashes unchanged. Inspected all 18 catalog images: captions have contrast, comparison letters are unobstructed, and all arrowheads fit. These are renderer checks; physical comprehension remains unqualified. The actual dark before screenshot is intact, but the expected-after attachment loses unchanged text and several unchanged sticker regions. This is unresolved visual evidence, not an accepted appearance or confirmed product defect. An additional diagnostic UI run captures both app and full-screen screenshots without changing product code: session 33401, Artifacts/t07-dark-capture-check.log/.xcresult. Restore simulator appearance to light after terminal completion. Full PR regression and commit remain pending.

Correction to the apparent dark-text loss above: raw PNG pixel checks found identical intact header regions before/after (including 4,994 light pixels in the solution header and 11,797 in the practice banner). Reopening the expected-after PNG alone displays the complete headers, stickers, diagram and anchors. Reopening the original N08 dark catalog alone also displays all captions. This was an inline multi-image display artifact, not missing data in the saved PNGs or a demonstrated application contrast defect. Do not count the explicit foreground styles as fixing a proven contrast bug. The arrow overlap and cropped arrowhead remain confirmed visual findings and are fixed.

Diagnostic capture run completed normally, exit 0, one UI test passed; all 213 source hashes matched. It preserves both app and full-screen captures. Simulator appearance was restored to light and UI teardown restored the original Reduce Motion setting. No build/test process remains live. Next: reduced-mode background/comparison/acknowledgement and layout qualification (narrow/landscape/largest text), then full PR regression, evidence/provenance updates and a coherent increment commit. Current work remains uncommitted; no release or T07 completion is claimed.

Extended the actual system Reduce Motion UI test through device Home/background, app activation, both comparison pictures with unchanged progress, explicit matches-before, replay completion and explicit acknowledgement. This is additional lifecycle coverage of existing implementation, not a fabricated missing-feature RED. The first expanded run is active as session 38516, Artifacts/t07-reduced-lifecycle.log/.xcresult; source hashes are captured. No Swift edits while live.

Expanded lifecycle run passed, exit 0, one test, no failures/skips; all 213 source hashes match. Added a landscape preview/explicit acknowledgement branch and guaranteed portrait restoration. Created the owned iPhone SE (3rd generation) simulator on the already installed iOS 18.5 runtime, DDC7E7F7-067A-46D3-AB8E-F5DC9A203BFC, to qualify the actual 375-point layout. The first narrow run is in progress, Artifacts/t07-reduced-narrow.log/.xcresult. This does not substitute for physical-device qualification.

The initial SE run finalized normally (exit 65) with one test-navigation failure: guide.align existed below the viewport but the test waited for hittability without scrolling. All 213 source hashes matched. This is not counted as product RED. Initial guide taps now use the same bounded reveal/scroll helper as comparison controls. The corrected navigation run is active as session 46425, Artifacts/t07-reduced-narrow-scroll.log/.xcresult; no product source was changed.

SE portrait/landscape navigation run passed normally (exit 0, one test, zero failures/skips), including background comparison and explicit advancement; all 213 source hashes match. The owned SE simulator is now explicitly set to accessibility-extra-extra-extra-large for the next existing UI flow, Artifacts/t07-reduced-largest.log/.xcresult. Its previous content_size query returned unknown on the fresh device; restore this owned test device to large after the run. No largest-text pass is claimed yet.

Setup correction: the content_size command above failed because Xcode had shut down the SE destination. The inadvertently started t07-reduced-largest run was explicitly stopped by SIGTERM to its owned xcodebuild PID 1980; the wrapper finalized exit 143. It is interrupted setup evidence, not a text-size run or product failure. Its log is preserved. Boot the owned destination, await boot completion, successfully set and read back the category, then run with parallel testing disabled to retain the configured destination.

Inspected SE portrait before/controls and the full-screen landscape controls capture: visible labels/cube/diagram and reachable controls after scrolling, including whole-cube acknowledgement. The app-only landscape attachment is incorrectly cropped; its simultaneous XCUIScreen capture is complete at 1334 by 750. Use the full-screen artifact for landscape review. The configured largest-text run is active as session 59424; boot completed and content_size successfully read back accessibility-extra-extra-extra-large before launch, with parallel testing disabled.

Configured largest-text run finalized exit 65 with one Settings-navigation failure at the offscreen Motion cell; CubeGuide was not yet opened. This is not a product defect or accessibility qualification. Added bounded scrolling for that Settings cell. The category still read accessibility-extra-extra-extra-large, and the corrected setup run is active as session 80541, Artifacts/t07-reduced-largest-navigation.log/.xcresult. Source hashes are captured; no Swift edits while live.

Largest-text app run finalized exit 65 with one failed Play navigation after comparison. Its exported hierarchy places Play above the viewport (y=-127, scroll viewport y=283), proving the one-direction test helper scrolled the wrong way. The helper now chooses up/down from element versus viewport position. Independent visual evidence confirms product layout defects: horizontal playback labels wrap into vertical fragments (Play 251.5 points high, Pause 398), and the fixed practice banner truncates. A focused UI regression bounds these short playback controls to 120 points high, allowing large type without glyph columns. Behavioral RED is active as session 26360, Artifacts/t07-largest-controls-red.log/.xcresult. Keep Swift unchanged until terminal completion; then implement adaptive control layout and address banner readability before claiming largest-text qualification.

The focused largest-text control regression finalized exit 65 at the intended assertion: Play height 251.5 exceeds the 120-point readability bound. All 213 captured source hashes match. GuideFlowView now chooses a vertical control layout for accessibility text categories and retains horizontal layout for standard categories, covering both playback and comparison controls. PracticeView allows its exact required banner wording to use its full vertical text size rather than truncate. Affected GREEN is active as session 70498, Artifacts/t07-largest-controls-green.log/.xcresult. The owned SE remains at the verified largest category during this run; restore large after terminal completion. Visual and full regression qualification are still required.

First adaptive-layout run finalized exit 65 before reaching playback assertions: the four-swipe helper overshot alignment (exported button y=209.5..387.5, viewport y=429.5..667). The source snapshot matches. This is not an adaptive-controls GREEN. The test now permits twelve bounded bidirectional slow swipes, reducing overshoot on the small viewport. The owned device was restored to large after the failed run; the next qualification must explicitly set/read back the largest category again. No full PR pass or commit yet.

Current live run: session 71172, Artifacts/t07-largest-controls-scroll.log/.xcresult. Largest content_size was successfully set and read back; parallel testing is disabled. Source hashes and the live handle are recorded beside the run. Resume this exact process, do not restart or edit Swift while live. Restore the owned SE to large when terminal; inspect final counts/assertions and screenshots. The adaptive controls/banner changes remain unverified and uncommitted. Full PR regression and all remaining T07/media/device/distribution gates remain open.

Largest-controls-scroll finalized exit 65: one failed UI navigation, no skips; all 213 source hashes matched. The new short-label height checks passed, but Play could not be reached. Exported synthesized gesture events start at y448.25, inside the visible navigation bar (y429.5–474), despite using the ScrollView accessibility frame. Repeated gestures therefore do not establish a product scrolling defect. The helper now clips its gesture region below visible navigation bars and drags within that region. A focused largest-text run is active as session 78625, Artifacts/t07-largest-gesture.log/.xcresult. Category was explicitly set/read back before launch; restore the owned SE to large after terminal completion. The original failure and portable summary/log remain retained. No product source changed in this diagnostic correction; full layout and PR qualification remain open.

Gesture-area correction run 78625 finalized exit 65. The full largest-text portrait path passed through preview, replay/pause, background, before/after comparison and explicit acknowledgement. Failure moved to the immediate landscape transition: sampled ScrollView geometry was x253.9/y-277/width172.3/height654 during rotation, producing a 3.88-point computed viewport. This is transient geometry evidence, not yet a settled landscape product verdict. Added a bounded expectation for landscape navigation/scroll widths and nonnegative scroll origin before interaction; existing action/progress assertions remain unchanged. All previous source inputs except the subsequently edited UI test still match the run snapshot. The owned simulator was restored to large after terminal failure, then explicitly configured/read back at the largest category for the new run. Session 29508 is running Artifacts/t07-largest-settled.log/.xcresult; keep Swift unchanged, poll the same handle, and restore large after terminal. No full PR pass or commit is claimed.

Settled-layout run 29508 completed exit 65; all 213 source hashes match. The landscape expectation passed, Play finished without changing progress, but acknowledgement remained unhittable after twelve alternating drags. Its final frame was y150.4..255.4 above the visible content y307..375. Recorded drags alternate y320.6→361.4 and back at the default 500 points/second, overshooting in the 68-point content region. Full-screen capture confirms readable practice wording but a small landscape content area; this is not accepted full visual qualification. The helper now uses an explicit 100-point/second drag and 0.3-second hold before release to remove inertial overshoot. No product code changed. Largest-category run 17842 is active at Artifacts/t07-largest-held-drag.log/.xcresult; keep Swift fixed and restore the owned SE to large after terminal completion. Prior failed logs, summaries, source verification, hierarchy and landscape screenshot are retained under t07/reduced-motion/.

Held-drag run 17842 finalized exit 65 during portrait alignment: removing momentum made the fixed twelve-drag budget insufficient for the long text layout. It is not a product RED or a pass. Separately, the settled landscape screenshot demonstrates that the fixed banner leaves less vertical room than one complete guide control. Added a focused installed-app regression requiring usable landscape content height at least the alignment control height, with exact practice wording retained. RED run 4512 is active at Artifacts/t07-banner-space-red.log/.xcresult, largest category verified. This new geometry assertion isolates the product layout constraint from long gesture navigation. No banner implementation edit until terminal behavioral RED.

Banner-space regression 4512 finalized exit 65 with exactly the intended 68 < 105 geometry failure, one executed test and no skips. All 213 source hashes matched. PracticeView now uses a horizontal banner in compact vertical size, with a semantic caption for the full required wording and an unwrapped Exit practice action. Portrait retains its headline/vertical layout; no Dynamic Type category or font scale is capped. The non-inertial test helper permits forty bounded drags for long scaled content. Both ReduceMotionUITests are running as session 46690, Artifacts/t07-banner-space-green.log/.xcresult, with largest category read back. Keep sources fixed and restore large after terminal. Visual inspection and full PR regression remain required before commit; no passed layout or release claim yet.

Initial banner GREEN 46690 completed exit 0: both layout and full system Reduce Motion UI tests passed, zero failures/skips, all 213 source hashes matched. The owned simulator was restored to large. Visual review then found the 180-point static comparison canvas still exceeded the largest-landscape 116.5-point content area. Expanded the focused layout test to require the entire static canvas to fit, with actual Reduce Motion enabled/restored. Behavioral RED 81069 completed one test with the intended 180 > 116.5 failure and unchanged source hashes. Its simctl diagnose collector was stopped only after all tests completed; diagnostic collection is partial, test result remains failed/exit 65.

To retain readable sticker labels, the cube stays at its existing size. The compact banner now applies the same scalable caption style to Exit practice and uses eight-point vertical padding; the exit target retains a 44-point minimum. Exact wording and portrait headline remain unchanged. Both focused tests are running as session 36409, Artifacts/t07-static-landscape-green.log/.xcresult; largest category was read back before launch. Keep Swift sources fixed, poll this handle, restore the owned SE to large when terminal, inspect full-screen images, then run the full PR suite before any commit. Expected full simulator discovery is now 49 cases (32 app integration/catalog and 17 UI). T07, media/device and distribution gates remain open.

Banner-only cube-fit attempt 36409 finalized exit 65: the full interaction test passed, but the dedicated cube-fit assertion still failed (180 > 132.5). All 213 source hashes matched; failed evidence is retained as static-landscape-banner-attempt. StaticGuideView now uses a 120-point canvas in compact vertical size and retains 180 points otherwise, following the specification allowance to shrink the 3D area at accessibility sizes. Sticker glyph size and exact drawing geometry are unchanged. Expanded the rendered catalog to all nine operations × light/dark × regular/compact height at explicit normal text size; this is visual evidence generation, not automatic approval. The actual system largest-text UI remains separately tested. Combined app integration/catalog and both ReduceMotionUITests are running as session 11746, Artifacts/t07-compact-static.log/.xcresult. Expected discovery is 34 tests. Keep Swift fixed; inspect terminal results and compact images, restore the owned SE to large, then perform full PR regression before commit.

Compact static run 11746 completed exit 0: 34 tests passed (32 app integration/catalog and two actual system Reduce Motion UI tests), zero failures/skips. All 213 captured source hashes matched. The owned SE simulator was restored to large. Individually inspected all 18 compact normal-text catalog images: captions, before/after stickers and separate direction arrows are visible with unclipped arrowheads. All 18 regular-height images are pixel-identical to the previously inspected catalog. Images and comparison hashes are retained in t07/reduced-motion/compact-catalog/. The largest-text UI tests separately establish the complete interaction path and that a 120-point canvas fits within the 132.5-point landscape content area. Full-screen captures retain readable practice wording; the large banner still requires scrolling through guidance. This is finite simulator coverage, not full physical accessibility, contrast or novice comprehension qualification.

Full PR regression is active as session 13176, Artifacts/t07-reduced-pr, console Artifacts/t07-reduced-pr-console.log. Completed stages passed 270 package tests, 45 Python tests and 33 host process-kill cases. Solver and clean simulator stages remain pending/running. Expected simulator discovery is 49 cases. Resume the same handle; keep Swift sources unchanged. Final regression results, source identity and commit remain required. T07, production media, physical qualification and distribution remain incomplete; nothing was uploaded or released.

Regression continuation: session 13176 remains live. The 10,000-state PR analysis passed with zero failures/timeouts; all 46,741 shallow states and eight named cases also matched their expected outcomes and independent replay. Reference comparison and simulator completion remain outstanding. git diff --check passed, and all 210 tracked/untracked implementation, test, tool, fixture and project inputs selected from the run snapshot remain unchanged. Documentation and screenshot evidence were updated while tested sources stayed fixed. This remains a partial regression result; no commit or release is claimed.

Regression checkpoint: session 13176 was polled live in the clean simulator UI stage. Both pinned-reference comparisons passed (10,000 PR and eight named cases); solver and storage reports are preserved under t07/reduced-motion/regression/. An intermediate source check confirms all 210 implementation/test/tool/project inputs unchanged. Full simulator completion, actual aggregate counts, final screenshots, final source verification and commit remain pending. Continue this handle without restarting or changing tested sources.

## Increment 4 final regression and scope

Full PR session 13176 completed exit 0. Artifacts/t07-reduced-pr/summary.json reports 49 simulator tests (32 app integration/catalog and 17 UI), zero failures/skips/expected failures and no structured runtime warnings. All 270 package tests, 45 Python tests, 33 host process-kill cases, 10,000 PR states, 46,741 shallow states, eight named cases and both pinned-reference comparisons passed. The 212 non-document inputs selected from the run snapshot are unchanged; source-verification.json records this final check. Console RealityKit shader diagnostics remain present and are not represented as physical graphics qualification.

All 36 final regular/compact light/dark catalog images are pixel-identical to the individually reviewed prior catalog. Inspected the final portrait before/after and full-screen landscape expected-after screenshots: letters, cube, separate direction cue and practice label are visible. The landscape screenshot is at a scrolled position, with the state caption above the viewport and controls partially below it; it is not evidence that the entire guide fits without scrolling. Actual UI tests separately passed scrolling, background/comparison, replay/pause, explicit advancement and exit. Earlier SE largest-text evidence remains a separate focused run. Portable results, compressed logs, hashes, image comparison and inspected screenshots are in t07/reduced-motion/regression/.

This coherent increment implements static Reduce Motion before/after guidance, signed direction diagrams, cancellable one-shot timing, pause/resume/replay and obsolete-callback rejection, system-policy comparison transitions, adaptive accessibility controls and compact practice/cube layout. It does not complete T07 or T08. Animated overlays, remaining original artwork, coordinated human narration/effects/haptics, camera integration, broader accessibility/contrast/physical/novice qualification and distribution remain open. No archive, TestFlight upload, submission or public release occurred.

## Increment 5 — animated instruction overlays (in progress)

Reduce Motion was committed as bfc82ee after the full regression pass. Added two actual-scene tests requiring nonempty visible highlight geometry on all nine front-layer bodies for turns and all twenty-six bodies for each of six regrips, plus a rendered direction cue. They also require cues to survive cancellation used by guidance preparation, while ordinary manual draft display removes them. This initial coverage checks presence, selection and lifecycle; exact direction geometry, endpoints, colors/letter obstruction and rendered visual qualification will need further tests.

The initial RED run is live as session 61962, Artifacts/t07-overlay-red.log/.xcresult, using fresh DerivedData and the app integration target. No overlay implementation has been added. Expected discovery is 34 app tests, including the two new cases. Confirm actual behavioral failures before implementing; a compiler or discovery failure is not RED evidence. Keep Swift sources fixed until terminal completion. T07 and all remaining product/media/device/distribution gates stay open.

Overlay RED finalized: the original exec handle is no longer available, no xcodebuild/diagnostic process remains, and the completed xcresult reports 34 tests, 32 passes and exactly two failures at missing highlights. All 147 captured source hashes match. Initial implementation adds cubie-attached outline rails and a fixed-view direction curve; cancellation retains instruction context and ordinary draft display removes it. Existing animated resting-count checks now require 26 cubies plus exactly one direction entity; manual-preview count checks remain 26. This first implementation is not visually qualified and remains uncommitted.

Initial overlay affected run is live as session 72026, Artifacts/t07-overlay-green.log/.xcresult, fresh DerivedData and expected 34 app tests. Source hashes are captured in t07-overlay-green-source.json. Keep Swift sources unchanged while it runs. The initial rails use a single unlit tint; light/dark contrast and camera occlusion are unqualified. Follow-up geometric tests must inspect actual arrow segment endpoints, signed direction and arrowhead plane for all nine operations, plus pose/lifecycle and bounded ownership. Full rendered review and PR regression remain mandatory before commit.

Initial affected GREEN completed normally, exit 0: all 34 app tests passed with zero failures/skips. All captured Swift source hashes match. Portable logs/source hashes and the final summary are in t07/overlays/. This qualifies initial overlay presence/selection/cancellation and existing affected regressions only. It does not qualify arrowhead geometry, actual rendered visibility/contrast, all overlay lifecycle boundaries or full PR regression. The increment remains uncommitted; no test/build process is live at this checkpoint.

Rendered-arrow geometry regression completed RED normally, exit 65 (t07-arrow-plane-red). The new test inspects actual beam endpoints, connected shaft segments and both arrowhead arms against literal signed endpoints/planes for all nine operations. All eighteen arrowhead plane assertions failed while shaft endpoints passed; prior source hashes are unchanged. The implementation now derives the curve plane from consecutive chords and constructs head width within that plane. Affected GREEN is live as session 8037, t07-arrow-plane-green.log/.xcresult, expected 35 tests. Keep Swift fixed until terminal. Visual appearance/contrast and full PR remain unqualified.

Arrow-plane affected GREEN completed normally, exit 0: all 35 app tests passed, zero failures/skips; captured source hashes are unchanged. Portable plane RED/GREEN evidence is in t07/overlays/. The next rendered catalog is additional coverage and manual-review input, not a claimed new behavioral RED cycle or visual approval.

Initial RealityKit snapshot catalog exited 0 with one test and 54 attachments (t07-overlay-visual), but every image contains exactly one RGB color, independently checked from the PNGs. Rejected as visual evidence. The test window was not associated with an application UIWindowScene; the next diagnostic run attaches it and rejects blank captures by inspecting downsampled image colors. No product rendering fix is inferred. Session 72189 runs t07-overlay-attached.log/.xcresult with fixed sources; visual qualification and full PR remain open.

Attached-window catalog completed normally, exit 0, one test and 54 nonblank images (minimum 6,620 distinct RGB colors), source hashes unchanged. Initial blank output was caused by the test window not participating in the app scene. Visual review REJECTS current product arrow placement: N04 light before hides the arrowhead behind the cube; N01 light midpoint crosses sticker letters. Two rejected examples and the review record are under t07/overlays/. Remaining images are not approved. No process is live. Next work must revise placement with visibility/non-obstruction regression coverage, inspect all nine operations in both appearances and run full PR before committing. The plane fix is geometrically qualified but the full overlay increment remains unfinished and uncommitted. No release occurred.

Separate-direction UI RED completed normally, exit 65: one test failed at the missing guide.directionDiagram after reaching animated guidance. Source hashes unchanged. Implemented the recorded placement revision: shared GuideDirectionDiagram below animated/static cube content, retained cubie highlight rails, removed rejected 3D curve/head implementation and its obsolete geometry/ownership assertions. Existing static signed-direction tests remain, and manual/animated resting roots again have exactly 26 bodies. The new installed UI requires animated mode, nonintersecting cube/diagram frames and unchanged progress through Play/Pause. Combined affected run is live as session 37936, t07-separated-arrow-green.log/.xcresult, expected 36 tests including both visual catalogs. No edits to Swift while it runs. This is not a passed visual review/full regression or commit.

Separated-direction affected GREEN completed normally, exit 0: 36 tests, zero failures/skips, source hashes unchanged. Inspected before/paused installed-guide screenshots: complete direction tip and explicit layer scope are below the 3D cube, with unobstructed sticker letters and readable controls in the tested light portrait layout. Shared static catalog comparison is recorded under t07/overlays/. Added existing-behavior coverage for highlight selection/ownership across all 24 poses and 18 moves through midpoint, endpoint and cancellation, plus actual body identity and manual cleanup. Fixed the snapshot helper buffer lifetime without changing product code. The affected run is live as session 12101, t07-overlay-lifecycle.log/.xcresult, expected 36 app tests. Remaining highlights catalog review, broader UI/layout and full PR remain required before commit.

Highlight lifecycle affected run completed exit 0: all 36 app tests passed, zero failures/skips, captured source hashes unchanged. Reviewed all 54 RealityKit before/mid/after light/dark captures using nine labeled sheets. Selection is visible and letters are unobstructed; the shared separate schematic remains independently covered. Original images, sheets, hashes and scoped review are in t07/overlays/catalog/. No measured contrast, physical comprehension or frame-rate claim is made. Full PR regression is the next required gate before commit.

Full PR regression is live as session 81494, Artifacts/t07-overlays-pr and t07-overlays-pr-console.log. Expected simulator discovery: 54 tests (36 app integration/catalog, 18 UI). Resume that handle without restarting or changing Swift sources. Full aggregate result, source verification, final current screenshots and coherent commit remain pending. T07, remaining original art, media/camera/device/accessibility and distribution are not complete.

Full-regression checkpoint: session 81494 was polled live. All 270 package tests passed (37 Core, 33 Solver, four Generator, ten Scan, 186 Session); initial Python stages and storage harness guard tests passed. All 216 non-document implementation/test/tool/project inputs match the run snapshot. Final storage, solver and simulator aggregate evidence remain pending. Prepared A07 completion-graphic behavioral boundaries in t07-rendering-plan.md without editing tested sources; begin that implementation only after overlay regression and commit.

Full regression continuation: session 81494 is confirmed live in the clean simulator stage. All 10,000 PR states and 46,741 shallow states verified with zero timeouts, eight named cases matched four valid/four invalid outcomes, and both pinned-reference comparisons passed. Reports are copied into t07/overlays/regression/solver/. All 216 non-document inputs remain unchanged. Existing RealityKit shader console diagnostics are present; final structured results and UI completion remain pending, so no full pass or commit is claimed.

Full PR FAILED: testManualCentersEditingAndRelaunch reported an app crash after returning from manual 3D preview and tapping Home. The 08:43:31 crash report shows main-thread EXC_BAD_ACCESS at null during SwiftUI ViewGraph/UIHostingView teardown; root cause is unconfirmed. Report and original Xcode log are retained under t07/overlays/regression/. Stopped the owned xcodebuild PID 37949 with SIGTERM after the failure; runner session 81494 finalized exit 143. Remaining UI tests are incomplete, not passed. No production source changes preceded diagnostic reproduction. The next run targets the unchanged failing UI path; full PR must pass afresh after diagnosis/fix. No commit or release.

Diagnostic reproduction is live as session 64212, Artifacts/t07-preview-crash-repro.log/.xcresult, fresh DerivedData, unchanged production sources and the existing exact failing manual UI test. Resume the handle; no source edits while live. A passing isolated run would not erase the crash or qualify the failed regression.

Isolated reproduction session 64212 completed exit 0: one unchanged manual UI test passed in 70.814 seconds. All 216 implementation inputs still match the failed full run. This does not reproduce or clear the crash. Next diagnostic uses Address Sanitizer and three explicit executions of the same path; outcomes remain diagnostic evidence, not an automatic retry that qualifies the original run. Root cause remains open.

ASan diagnostic is live as session 29746, Artifacts/t07-preview-asan.log/.xcresult, fresh DerivedData and three explicitly requested test iterations. Resume this process without source edits or concurrent builds. The failed full PR remains failed; no commit/release qualification follows from an isolated diagnostic pass.

ASan diagnostic session 29746 completed normally, exit 0. All three explicit executions passed (71.667, 71.765, 72.925 seconds) without reported Address Sanitizer errors. Original crash remains unresolved; this does not clear the failed full regression. Next diagnostic concentrates repeated manual-preview dismissal and Home/Resume transitions in one app process. No speculative production fix has been applied.

Added a bounded regression that creates a real partial manual draft, then performs 20 preview-open/dismiss/Home/Resume cycles in one app process, requiring the original Blue sticker, missing count and running foreground app after every cycle. No mocked renderer or production behavior change. Run is live as session 81988, Artifacts/t07-preview-cycles.log/.xcresult with captured sources. This targets the observed teardown failure; a nonreproduction will be recorded honestly rather than called a fix. Full PR and original crash investigation remain open.

Repeated preview regression reproduced an app crash on cycle 2 while opening the next preview. Run finalized exit 65. The 08:52:10 report differs from the first SwiftUI stack: SIGABRT, malloc reports freeing a pointer that was not allocated, with RealityKit AssetManager.processDeferredPointerUnload at the top. Both reports are retained; a shared root cause is not yet established. Source hashes unchanged. This is an actionable reproducer; next run applies ASan to this exact repeated path.

Repeated-path ASan run is live as session 57593, Artifacts/t07-cycles-asan.log/.xcresult. Keep source fixed and resume this exact handle. Resource ownership/lifetime is a diagnostic hypothesis, not a confirmed fix or attribution to application/framework code. Full regression remains failed and the overlay increment uncommitted.

Repeated-path ASan diagnostic completed: the terminal xcresult reports one passed test, all 20 cycles completed in 222.661 seconds, no reported sanitizer error. No xcodebuild process remains. This does not reproduce or resolve either ordinary-build crash. Preserved summary/log under overlays/regression. Investigating allocation and scene teardown ownership before any production change.

Allocator diagnostic is live as session 94246, Artifacts/t07-preview-malloc.log/.xcresult. A wrapper temporarily supplies MallocScribble and MallocStackLogging to the existing repeated-preview UI test; production sources are unchanged, and the wrapper restores the UI test in finally with SHA-256 proof. Do not edit Swift or start another build until that handle is terminal and restoration is verified. A passing diagnostic cannot clear the original crashes.

Allocator diagnostic 94246 completed exit 0: one passed test, all 20 cycles, no failure. Wrapper restored the UI test byte-for-byte (restoration proof retained). The instrumented allocator did not reproduce the ordinary-build crash. Next isolation adds real rendered scene creation/teardown without SwiftUI navigation; no speculative production fix has been applied.

Added testRepeatedRenderedSceneTeardown: 20 actual nonblank RealityKit renders of the same partial draft, initial representable-style update, production dismantle, and weak model-release check. It uses UIKit windows without SwiftUI navigation/session transitions to isolate the failing boundary. Run 60291 is live at Artifacts/t07-scene-teardown.log/.xcresult, expected one test, sources captured. Keep Swift fixed until terminal. This is diagnostic coverage of existing behavior, not a claimed feature RED or fix.

Direct rendered-scene diagnostic 60291 completed exit 0: one passed test, 20 nonblank renders and released model owners in 6.640 seconds. All captured sources unchanged. This rules out a simple retained model in that UIKit-only path, not SwiftUI/navigation failure or all framework resource retention. Preserved summary/log/source hashes. Next ordinary-build diagnostic repeats the original UI path with all allocator/ASan instrumentation disabled; retain every iteration outcome rather than treating a later pass as clearing the failed PR.

Ordinary-build repeated-path diagnostic is live as session 1591, Artifacts/t07-preview-ordinary.log/.xcresult, three explicit iterations of 20 cycles. ASan is disabled and the UI test has no malloc launch variables. Expected executed count is three. Keep Swift sources fixed and resume this handle; do not restart on an observation timeout. The original full PR remains failed and the overlay increment uncommitted.

Ordinary-build iteration 1 reproduced a crash after preview dismissal and Home on cycle 8 (104.968 seconds). The 09:07:23 report shows EXC_BAD_ACCESS at 0x4049000000000000 in objc_loadWeakRetained/NSConcretePointerArray/_UIUpdateCycleUnregisterIdleObserver/HostingScrollView.didMoveToWindow. Preserved as ordinary-iteration1-cycle8-crash.ips. This differs from the earlier RealityKit unload and ViewGraph stacks; common cause is still unconfirmed. All captured sources unchanged. Session 1591 continues iterations 2 and 3; no production edits or passing aggregate claim.

Ordinary diagnostic 1591 finalized exit 65: all three iterations executed, two crashes (cycles 8 and 20) and one full pass. Iteration 2 failed in RealityKit FramePersistentPtr.reset/MeshComponent.updateMaterialData; both reports and aggregate summary/log/source hashes are preserved. All sources unchanged. The passing third iteration does not clear reproducible crashes. Next: Guard Malloc on the same UI reproducer, without product changes, seeking the invalid access before downstream teardown. Apple malloc debugging guidance and installed libgmalloc manual confirm this simulator diagnostic and DYLD_INSERT_LIBRARIES mechanism.

Guard Malloc diagnostic is live as session 59197, Artifacts/t07-preview-guard.log/.xcresult. The wrapper temporarily injects /usr/lib/libgmalloc.dylib and stack logging through the UI test launch environment, with source restoration and hashes in finally. No production source changed. Keep Swift fixed until terminal and verify restoration. Diagnostic activation must be checked; a setup/loader failure is not the target crash.

Guard activation verified in live app PID 50601: the process sample lists libgmalloc.dylib and executing GuardMalloc allocation/free functions. Preserved sample and activation JSON. vmmap inspection failed with mach_ports_register invalid right; it was not used as activation evidence. Session 59197 remains live; no result or fix claimed.

Guard run 59197 finalized: Xcode exit 65, one failed test on opening the first preview. Source wrapper restored the original test bytes. Guard activation was proven; the crash is EXC_BAD_ACCESS in RealityKit AudioPlayerSystem.update at unmapped 0xbbbdb600b8. The app has no instruction-audio implementation. A shared cause with ordinary crashes is unconfirmed. malloc_history cannot inspect the terminated PID, so next diagnostic must capture allocation history while a debugger holds the faulting process. Logs, summary, crash, activation and restoration proof preserved.

First live-debugger capture attempt completed with Xcode exit 65 and restored UI-test bytes. LLDB attached successfully, but its startup pause caused the existing five-second Orange-control wait to fail before preview; process then exited SIGKILL. This is diagnostic setup failure, not target-crash evidence. Retained summary/log/debugger/restoration proof. Next temporary diagnostic adds a 60-second attachment window after launch; normal UI assertions/timeouts and product behavior stay unchanged.

Guard-held diagnostic is live: test wrapper session 35054 and debugger watcher session 31128, Artifacts/t07-preview-guard-held*. The test has a temporary explicit 60-second post-launch attachment window. Both processes belong to this diagnostic; preserve them until terminal and verify wrapper restoration before any Swift edit/build/commit. No production change or root-cause claim.

Guard-held diagnostic finalized: Xcode exit 65, debugger exit 0, original test restored. LLDB caught CoreRE AudioPlayerSystem.update +8948 executing strb wzr, [x8, #0x1a0] to unmapped 0xb78c6f40b8 (x8=0xb78c6f3f18) on first preview. This is an observed invalid write inside framework code; the reason for the invalid target and relation to ordinary crashes remain unproven. Live malloc_history also failed with mach_ports_register invalid right, so no allocation/free provenance was recovered. Preserved full debugger stack/registers/log and restoration evidence. Next controlled diagnostic replaces only manual preview rendering with a bare non-AR ARView, retaining navigation and Guard Malloc, to test whether cube geometry/resources are necessary to trigger the invalid write. This is temporary isolation, not a product replacement.

Bare-ARView control is live as session 53057, Artifacts/t07-preview-empty-ar.log/.xcresult. A wrapper temporarily replaces only the manual CubeSceneView surface with an empty non-AR ARView and injects Guard Malloc through the UI test. It creates no cube model/mesh/anchors and performs no scene updates; all navigation/draft assertions remain. Restore both files byte-for-byte before further work. This cannot qualify production visuals or full regression.

Bare-ARView control 53057 finalized exit 65, one failed test on first preview. The crash report lists libgmalloc and the same AudioPlayerSystem.update +8948 invalid write, despite no cube model, meshes, anchors or update code. Both temporary files restored exactly. This isolates the guarded failure from cube geometry, but not yet all CubeGuide/SwiftUI context or ordinary-run corruption. Next reduction is an Artifacts-only UIKit/ARView executable with no CubeGuide/package/SwiftUI code; it is diagnostic tooling, not a second product target or shipped app.

Standalone UIKit-only probe reproduced the same guarded AudioPlayerSystem.update +8948 invalid write on iOS 18.5 (PID 54889). It contains only an app delegate, window/controller and one empty non-AR ARView: no CubeGuide, packages, SwiftUI, scene entities or audio code. Source/build/launch/crash evidence is in overlays/regression/minimal-ar. simctl launch exited zero despite the app crash; the .ips and absent process establish failure. The direct compiler emitted a sysroot warning; load commands explicitly show IOSSIMULATOR/minimum 18.0/SDK 27.0, and installation/launch succeeded. This establishes the guarded framework failure independently of product code, not yet the causal link to ordinary-build corruption or physical-device behavior. Next compare the same binary against another explicitly inventoried installed simulator runtime.

Runtime control: created owned iPhone 16 Pro/iOS 26.5 simulator 610EA507-710F-4AA6-943C-AF57C0035F07. The exact same UIKit-only binary ran with Guard Malloc for 67 seconds, with ARView/CoreRE render callbacks observed in a sample, then was intentionally terminated. No immediate guarded failure in that finite observation. This narrows the observed minimal failure to the tested 18.5 runtime compared with 26.5; it does not certify minimum-OS product behavior or identify the ordinary crashes as the same defect. Next standalone probe repeats bare ARView creation/teardown under the ordinary 18.5 allocator.

Strengthened standalone ordinary-allocator probe asserts each ARView is window-attached and its weak reference is nil after teardown. It crashed on cycle 2 on iOS 18.5 (09:35:20 .ips: objc_retain/NSConcreteMapTable/BSSettings during UIKit window updates), after reporting the first presented/released cycle. Source/build/log/crash preserved under minimal-ar/ordinary. The earlier 100-loop probe did not explicitly check attachment/release and is not used to qualify those conditions. Two independent immutable controls now run: plain UIView on iOS 18.5 (session 46738), and the exact strengthened ARView binary on iOS 26.5 (session 53761), 100 cycles each. No product source or test mutation remains.
